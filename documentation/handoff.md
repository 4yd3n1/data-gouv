# Handoff — data-gouv Civic Intelligence Platform

> Updated: May 7, 2026 (Session 68 — HATVP Documents tab simplification: canonical DI + DSP surface via `<HatvpDeclarationsSection>`, separating public income, non-public income, elected mandates, assets, passif, and net patrimoine). Current phase: **UX Restructure complete (Phases 1-6 done)** + Phase 9 Government Profiles + **Deep Government Investigation** + **Bilan Macron Dossier** + **HATVP Normalization & Refresh** + **Décrets de déport (Session 47)** + **Investigative front-page redesign (Session 48)** + **Homepage polish & Bilan reframing (Session 49)** + **Variant A design system (Session 50)** + **`/dossiers/medias` redesign (Session 50 addendum)** + **Map palette fix + Bilan final corrections + DOM poverty fallback + `/signaux?type=lobby` dashboard rework (Session 51)** + **`/profils/lobbyistes/[id]` investigative profile upgrade (Session 52)** + **Lobbyist ownership & government-ties layer pilot (Session 53)** + **Rémunérations panel — HATVP public-office income aggregator on every profile (Session 54)** + **HATVP source cache rename + ingest-hatvp dedup fix (Session 55)** + **Indemnité officielle (barème décret 2002-562 + 2012-983) on Rémunérations panel (Session 56)** + **Souveraineté économique dashboard `/souverainete` + minister profile cross-link (Session 61)** + **Pre-2027 civic-virality strategy plan + 5 P0 subplans (Session 63 — planning)** + **Pre-2027 launch wave shipped (Session 64)** + **Unified HATVP Dossier data layer + DSP ingestion (Session 65)** + **Investigation-surface transformation: 5-section profile frame + dashboard homepage + signal registry + trust layer + search investigation cards (Session 66)** + **Hybrid homepage + sitewide photo backfill (Session 67)** + **Clean HATVP DI/DSP Documents UX (Session 68)**.

---

## What This Is

French civic intelligence platform. Next.js 16 App Router + Prisma 7 + PostgreSQL 14. Ingests open data from data.gouv.fr, INSEE, HATVP, Sénat, Assemblée Nationale. Goal: **maximum transparency through public data**. "Intelligence Bureau" aesthetic (dark, teal accents, Instrument Serif headings).

Full blueprint: [`ARCHITECTURAL-PLAN.md`](../ARCHITECTURAL-PLAN.md)

---

## Current State — Phases 1–8 + Phase 9 + UX Restructure Complete (Sessions 33–66)

~1M rows across **57 Prisma models** (`grep -c "^model " prisma/schema.prisma` ground truth — earlier handoffs reported 44/46 due to Session 53's lobbyist-ownership trio not being added to per-layer counts; Session 47 added `DecretDeport`; Session 53 added `LobbyisteDirigeant` + `LobbyisteDirigeantCarriere` + `LobbyistePosition`; Session 61 added `AcquisitionEtrangere`; **Session 64 (Pre-2027 launch wave) added `ErrorReport` + `NewsletterPending`**; **Session 65 added `DeclarationPatrimoine` + `PatrimoineRow` (HATVP DSP layer)**). **~34 active routes** (Session 66 added `/corrections`), (29 legacy deleted Session 43, +1 bilan-macron Session 45, +1 methodologie Session 50, +1 souverainete Session 61, **+5 from Pre-2027 launch wave**: `/apropos`, `/signaler-une-erreur` + `/merci`, `/v/[slug]`, `/v`, `/mon-depute`) + **9 OG image routes** (3 new: `/profils/[slug]`, `/signaux`, `/dossiers/[slug]` plus `/v/[slug]` + `/mon-depute`) + **5 Twitter card routes** (root + 4 same routes — all new). **5-item nav unchanged** (Session 50): Dossiers | Signaux en Direct | Annuaire | Territoire | Méthode. Homepage is hybrid Variant A editorial top + civic dashboard below (Session 67). `/signaux?type=lobby` is a dedicated 5-section lobby dashboard (Session 51); `/profils/lobbyistes/[id]` is a Variant A investigative profile ported to the same data layer (Session 52). `/souverainete` is a 6-figure dossier on French strategic-asset losses 2014-2026 (Session 61) — `DossierNav` extended to 4 pills, footer Accès column updated. **Minister Documents tab is now canonical DI + DSP** via `<HatvpDeclarationsSection>` (Session 68), separating public income, non-public income, elected mandates, patrimoine assets, passif, and net. **JSON-LD** (`Organization` at root, `Person` + `GovernmentPosition` on profile routes — Session 64+66). **`robots.ts` + `sitemap.ts`** (8 166 URLs, env-driven hostname via `NEXT_PUBLIC_SITE_URL`). **Cross-repo `viral-schemas` package** at `/Users/aydenmomika/packages/viral-schemas/` consumed by both data-gouv (`/v/[slug]` manifest reader) and the sibling Remotion repo (`scripts/generate-fact-check.ts` 4 LLM-assisted providers, sourcing gate untouched). **`remotion_reader` Postgres role** SELECT-only. `pnpm build` clean as of Session 68.

| Layer | Models | Rows |
|-------|--------|------|
| Territory | Region, Departement, Commune | 37,031 |
| Governance | Depute, Senateur, MandatSenateur, CommissionSenateur, Lobbyiste, ActionLobbyiste | ~106,500 |
| Parliament | Organe, Scrutin, GroupeVote, VoteRecord, Deport, ScrutinTag, ConflictSignal | 5,831 scrutins + ~973K votes |
| Parliamentary Laws *(Session 33)* | LoiParlementaire, ScrutinLoi | 19 lois, 2,589 links |
| Local | StatLocale (~1,644), BudgetLocal (69,023), StatCriminalite, DensiteMedicale | varies |
| Culture | Musee, FrequentationMusee, Monument | ~60,071 |
| Elections | Elu, ElectionLegislative, CandidatLegislatif, PartiPolitique | ~601,514 |
| Declarations *(re-ingested Sessions 42, 46)* | DeclarationInteret, ParticipationFinanciere, RevenuDeclaration | 10,226 decl + 10,936 part + 381,088 rev = **~400K rows** (Session 46 — HATVP dropped retired officials from their merged.xml, so total shrunk but coverage of current ministers is higher) |
| Gov. Profiles *(Phase 9 + Sessions 34, 44, 46, 47, 55)* | PersonnalitePublique, MandatGouvernemental, EntreeCarriere, InteretDeclare, EvenementJudiciaire, ActionLobby, **DecretDeport** | 110 persons, 49 mandats, 474 career, **15 judicial**, **2,102 HATVP interests covering 67 personnalites** (Session 55 dedup fix; was 1,988 after Session 46), 131,842 ActionLobby, **11 DecretDeport** (Session 47) |
| Lobby Ownership *(Session 53 — pilot)* | **LobbyisteDirigeant**, **LobbyisteDirigeantCarriere**, **LobbyistePosition** | 6 dirigeants (editorial seeds for Lysios/Anthenor/FNMF/Com'Publics), 27 carrière entries, 6 positions (all `verifie=false` pending editorial review) |
| Media Ownership *(Session 35)* | GroupeMedia, MediaProprietaire, ParticipationMedia, Filiale | 10 groups, 10 owners, 10 participations, 72 filiales |
| Souveraineté économique *(Session 61)* | **AcquisitionEtrangere** | 35 phases / 28 distinct cases (2014-2026) — hand-curated, sourced corpus of foreign acquisitions, vétos IEF, sauvetages, rachats étatiques |

| Cross-references *(Session 37–38)* | nuance-party-map, conflict drill-down, commission-lobby overlap | static mappings + runtime queries |
| Historical govts *(Session 37, not yet seeded)* | 4 data files: Borne (36), Attal (35), Barnier (41), Lecornu (36) | 148 members prepared |

| Signals *(Session 38, recalibrated 41)* | signal-types.ts, signal-card.tsx | 6 signal types computed at query time (no new models). Severity thresholds recalibrated Session 41. |
| UX Restructure *(Sessions 40+43)* | /profils hub, /signaux filters, vote-tags.ts, slug.ts | ~25 active routes (was 62), 4-nav, search-first homepage. Phases 1-6 complete. |
| Voting Overhaul *(Session 41)* | ScrutinAccordion, scrutin breadcrumbs, par-sujet filter, LoiCard, search URLs | Role-grouped amendments, parent law links, amendment filter on par-sujet |
| Declaration Audit *(Session 42)* | audit-declarations.ts, hatvp-parser.ts, case-insensitive fixes | 3-phase audit (XML vs DB vs display). 1,973/1,973 perfect matches. Fresh HATVP re-ingestion. |
| Route Consolidation *(Session 43)* | vote-tags.ts, /votes/scrutins/[id], /territoire/economie, /territoire/comparer | 29 legacy pages deleted, all redirects active, scrutin data refreshed to Mar 2026 |
| Deep Investigation *(Session 44)* | analyze-vote-contradictions.ts, analyze-lobby-exposure.ts, PORTFOLIO_KEYWORDS expanded | 12 parallel investigation agents (200+ web searches), 24 conflict flags, vote contradiction alerts, revolving door badges, lobby-career overlap highlights |

**Client components** (14): `SearchInput`, `SearchBox`, `Avatar`, `DeclarationSection`, `ProfileTabs`, `DeptLookup`, `NavSearch`, `FranceMap`, `DeltaBadge`, `GroupExpander`, `ScrutinAccordion`, `MediaBoard`, `MobileNav`, `ConflictDrilldown`.

> Session-by-session details below in `## Recent Sessions`.

---

## How to Run

```bash
pnpm dev              # Dev server (auto-uses Node 20.19.2 via .nvmrc)
pnpm build            # Production build — catches TS errors
pnpm db:migrate       # Run Prisma migrations
pnpm db:generate      # Regenerate client (required after every migration)
pnpm ingest           # Full ingestion (all waves, ~10 min, idempotent)
pnpm ingest:insee-local  # INSEE Mélodi (5 datasets × 101 depts — re-run safe)
pnpm compute:conflicts   # Populate ConflictSignal (run after tag-scrutins)
pnpm refresh:search      # Refresh search_index materialized view

# Phase 9 (run manually — not yet in pnpm ingest)
pnpm seed:gouvernement                          # Seed 4 governments (Borne/Attal/Barnier/Lecornu, 148 members total)
npx tsx scripts/generate-carriere.ts             # Auto-generate EntreeCarriere from mandats + Depute/Senateur
npx tsx scripts/ingest-hatvp.ts                  # Ingest HATVP XML declarations (re-run safe)
npx tsx scripts/ingest-agora.ts                  # AGORA lobby → 131,842 ActionLobby (20 ministry codes)
npx tsx scripts/ingest-research-output.ts --all  # Career + judicial from data/research-output/*.json

# Audit (read-only — run anytime to verify data integrity)
pnpm audit:declarations                          # 3-phase HATVP audit: XML vs DB vs display (JSON report)

# Session 33 (run manually)
npx tsx scripts/seed-lois.ts           # Seed 19 LoiParlementaire + 2,589 ScrutinLoi links (idempotent)

# Session 35+37 (run manually)
pnpm seed:medias                       # Seed media groups + owners + état-français gov link (idempotent)
```

---

## Critical Quirks

### Schema field traps (wrong = TypeScript build errors)
- `{ prisma }` is a **named** export — `import prisma from "@/lib/db"` = build error
- `pnpm db:generate` required after every migration (build fails with "Property X doesn't exist on PrismaClient")
- `Scrutin.votes` (relation), `Scrutin.dateScrutin`, `Scrutin.sortCode`, `Scrutin.abstentions`
- `GroupeVote.abstentions` — no `nomOrgane` field (use `organeRef`)
- `Depute.groupe` (NOT `groupePolitique`); `Depute.departementRefCode` for joins (NOT `departementCode` — that can be `"099"`)
- `Lobbyiste.nom`, `Lobbyiste.categorieActivite`
- `Elu.nom`/`Elu.prenom`, `Elu.codeDepartement`
- `DensiteMedicale.specialite` (NOT `profession`)
- `EvenementJudiciaire` has no `createdAt`/`updatedAt` columns
- `ElectionLegislative`: no FK to `Departement` — overseas codes (`"ZZ"`) don't exist in COG; `codeDepartement` is plain string

### Operational state
- **`NEXT_PUBLIC_SITE_URL`** (Session 64) — canonical hostname read by `robots.ts`, `sitemap.ts`, JSON-LD `url`/`@id` fields, OG/Twitter card URLs, `<link rel="canonical">`, `metadataBase` on root layout, share-button URL construction. Falls back to `http://localhost:3000` for dev. **Bake the real production hostname into the deployment env BEFORE first prod deploy** — search engines cache `<link rel="canonical">` and JSON-LD `url`, and the wrong value is hard to reverse.
- **Cross-repo `viral-schemas` workflow** (Session 64) — after any change in `/Users/aydenmomika/packages/viral-schemas/`: (1) `pnpm --filter viral-schemas build` (or `cd packages/viral-schemas && pnpm build`); (2) `pnpm install` in BOTH consumers (data-gouv + Remotion); (3) Remotion's `pnpm prisma generate` if the schema symlink target moved.
- **`remotion_reader` Postgres role password** — currently `remotion_reader_local_only` placeholder (in BOTH the SQL migration and `/Users/aydenmomika/remotion/remotion/.env.local`). Rotate before this leaves the developer's machine.
- **`ANTHROPIC_API_KEY`** in `/Users/aydenmomika/remotion/remotion/.env.local` is a placeholder. Set the real key to unblock end-to-end LLM draft generation. Until then `npx tsx scripts/generate-fact-check.ts personnalite:<slug>` fails gracefully at the LLM call boundary; Prisma query, prompt construction, and SHA-256 cache key all execute correctly.
- **`prefers-reduced-motion` global gate** (Session 64) — `globals.css` `@media (prefers-reduced-motion: reduce)` block disables `animation`/`transition` on `.fade-up`, `.bar-fill`, `.live-dot`, `.live-dot-amber`, `.pulse-dot`, `.sigint-section::after`, `.pulse`. New animation utilities should be added here AND get a non-animated fallback.
- **WCAG AA color contrast** (Session 64 polish wave) — body-text token values: `--color-fg-dim` `#727d93` (4.63:1 against `#0a0f1a`), `--color-bureau-400` `#7a8399` (5.05:1). `--color-fg-faint` `#3f4860` (2.10:1) is decorative-only — never use on text. Don't darken these tokens without re-verifying the contrast ratio.
- **`<dl>` direct-children rule** — every `<dl>` must contain ONLY `<dt>` and `<dd>` as direct children (axe `definition-list` rule). Wrap supplementary elements (e.g. `<SrcChip>`) INSIDE the `<dd>`. Pattern violation broke the launch-gate Lighthouse a11y score on `/v/[slug]` until fixed.
- **Tap-target minimum 44×44 px** — `share-button.tsx`, `.mobile-menu-btn`, `france-map.tsx` close button all corrected. New icon-only buttons should add `min-h-[44px] min-w-[44px]` (or padded equivalent) and an `aria-label` if no visible text.
- **Node 20.19.2+** required (Prisma 7 WASM, `.nvmrc` pins it, `pnpm dev` auto-switches)
- `ingest:elus` needs 8GB heap (`NODE_OPTIONS=--max-old-space-size=8192`)
- ARM communes (Paris/Lyon/Marseille): `postal-resolver.ts` replaces ARM codes with parent COM via `comparent`. Always filter `typecom: "COM"` for counts
- `ConflictSignal` is empty until `pnpm compute:conflicts` runs (UI has graceful fallbacks)
- `search_index` view requires `pnpm refresh:search` after route changes (canonical `/profils/*` + `/votes/scrutins/*` URLs since Session 43)
- **Deputy photo URL pattern** (Session 67): `https://www2.assemblee-nationale.fr/static/tribun/17/photos/{numericId}.jpg` (strip `PA` prefix from `Depute.id`). Legacy `/dyn/deputes/{id}/image` returned 404. Sénat URL pattern fully gone — `Senateur.photoUrl` is intentionally null; senators fall back to initials via `Avatar`. PersonnalitePublique photos sourced from local files + Wikipedia REST page-summary; see `scripts/fetch-wikipedia-portraits.ts` and `documentation/photo-credits.json` for provenance.
- `lucide-react` is intentionally **NOT** installed — use inline SVG everywhere

### HATVP pipeline
- **Name normalization mandatory** (Session 46): all cross-dataset name joins MUST use `nomNormalise` + `prenomNormalise` (compound btree-indexed on `Depute`, `Senateur`, `DeclarationInteret`, `PersonnalitePublique`). Source of truth: [`src/lib/normalize-name.ts`](../src/lib/normalize-name.ts) (NFD + strip U+0300-U+036F + lowercase + trim). Ingestion scripts populate on write; backfill via `pnpm tsx scripts/backfill-normalized-names.ts`. **NEVER** `{ equals, mode: "insensitive" }` — handles case but not accents.
- **HATVP merged.xml** is gitignored (~120MB), cached at `documentation/source-cache/declarations.xml`. HATVP server is flaky — re-download with `curl -C - --retry 30 --retry-delay 3 --retry-all-errors -o <path> https://hatvp.fr/livraison/merge/declarations.xml`. `ingest-hatvp.ts` prefers the local cache.
- HATVP XML parser: `fast-xml-parser` (NOT xml2js). Stream — files can be 200MB+
- 8 of 37 Lecornu II ministers genuinely haven't filed HATVP (Macron + 7 secretaries)
- **Profile Documents tab (Session 68)**: canonical renderer is `<HatvpDeclarationsSection>` on `?tab=documents`. It consumes `getDossierData()` and must keep DI and DSP separate: DI = interests/activities/revenues; DSP = assets/passif. Non-public income (books, private jobs, consulting, publisher advances) must be badged `Non public` and must not be summed into "public/government functions". Do not call declared HATVP amounts "salary certified" or "employer cost".

### Routes & nav (current as of Session 49)
- All legacy routes deleted: `/gouvernance/*`, `/representants/*`, `/gouvernement/*`, `/comparer/*`, `/economie` → HTTP 308 redirects in `next.config.ts`
- Canonical: `/profils/*` for all people; `/votes/scrutins/[id]` for scrutin detail; `/territoire/economie` and `/territoire/comparer`
- **4-item nav**: `Dossiers` → `/dossiers/bilan-macron` (acting hub) | `Signaux en Direct` → `/signaux` | `Annuaire` → `/profils` | `Territoire` → `/territoire`
- `/signaux` URL filters: `?type=conflits|portes-tournantes|lobbying|medias|declarations|dissidences` + `?severity=CRITIQUE|NOTABLE|INFORMATIF`
- `/votes/par-sujet/[tag]` defaults `?vue=final` (excludes amendments); toggle to `?vue=tous`
- **Homepage** (Sessions 48-49): asymmetric `[minmax(0,1fr)_380px]` grid, NOT `max-w-7xl grid-cols-12`. Editorial column capped `lg:max-w-[56rem]`. Decoration purged — typography carries hierarchy. See `.claude/rules/frontend.md` for layout rules.

### Profile UI signals (Session 44)
- `CareerSection` revolving door badge: `matchRevolvingDoor()` flags `CARRIERE_PRIVEE` orgs matching `PORTFOLIO_KEYWORDS` for current `ministereCode`
- `ParliamentarySection` vote contradiction alert: queries `VoteRecord` for "contre" votes on `MINISTERECODE_TO_TAGS`-mapped scrutins
- `LobbySection` "Ancien employeur" tag: cross-references `EntreeCarriere` `CARRIERE_PRIVEE` orgs against top lobby orgs
- Only **6/17 deputy-ministers** currently have `PersonnalitePublique.deputeId` linked — alerts only fire for linked ministers; remaining need name-matching pass
- **Profile sections must be self-contained components** (`<HatvpDeclarationsSection />`, `<InteretsSection />`, `<LobbySection />`, etc.) imported into `page.tsx`, never inline section logic
- **`EvenementJudiciaire.verifie` guard**: NEVER display unless `verifie = true`

---

## Next Steps

### Immediate — Pre-2027 launch wave (Session 64) follow-ups

User-side actions:
1. **Set `ANTHROPIC_API_KEY`** in `/Users/aydenmomika/remotion/remotion/.env.local`. Pipeline up to the API call is wired; until then `npx tsx scripts/generate-fact-check.ts personnalite:<slug>` fails gracefully at the LLM-call boundary. Generate first ~10 drafts on Opus 4.7 to baseline quality, then evaluate `--model claude-sonnet-4-6` downgrade.
2. **Rotate `remotion_reader` password** before this leaves the developer's machine — change in BOTH the SQL migration and Remotion `.env.local`.
3. **Manual Google Rich Results test** post-deploy on `/profils/<top-minister-slug>` to confirm the new `Person` + `GovernmentPosition` JSON-LD is indexable.
4. **Live Lighthouse a11y re-run** post-deploy to verify the contrast / target-size / `<dl>` fixes land at ≥ 95 (verifier couldn't re-score offline).
5. **Bake the real `NEXT_PUBLIC_SITE_URL`** into the deployment env BEFORE first prod deploy (search engines cache canonical URLs and JSON-LD `url` fields).

Code/data follow-ups:
6. **Build commune → circonscription mapping**. `/mon-depute?cp=75008` resolves a single commune (Paris) and renders all 18 département députés because `Depute.circonscription` (number) isn't joined to commune codes. Disclaimer + plural metadata fix the editorial inconsistency, but the real fix is a data ingestion: parse `ElectionLegislative` results joined to commune codes, OR ingest the Min. Intérieur circonscription→commune CSV. Multi-circo communes affected: Paris (18), Marseille (7), Lyon (14), Toulouse (5), Nice (4), Bordeaux (3).
7. **LCP rewrite for `/v/[slug]`**: 4.4s LCP — root cause is `getDraft()` falling into an O(n) full-scan with per-file Zod validation when slug doesn't filename-derive in step 1. Build-time slug→filename index would fix; needs integration test coverage before shipping (TODO comment in place at `src/app/v/[slug]/page.tsx`).
8. **Draft schema fields**: new drafts (Remotion side) should include `slug` (kebab), `_meta.published: true`, `personnaliteSlug` when first drafting via the LLM pipeline. Filename-derived fallback works for the existing 5 but is not load-bearing forever. Once the first new draft with `_meta.published: true` ships, also add `/v` to `STATIC_ROUTES` in `src/app/sitemap.ts`.
9. ~~**Track down `scripts/ingest-dsp-pdf.ts`**~~ → **Resolved Session 65.** Script was finished, schema added (`DeclarationPatrimoine` + `PatrimoineRow` migration `20260502132628_add_declaration_patrimoine`), parser hardened with `description as object` cast for the JSON field. Build is clean. 44 DSPs ingested, 1 013 rows.
10. **Typography sweep on dossier section components** (`src/components/bilan/*`, `src/app/dossiers/sante/page.tsx`) — outside Session 64's 5-file scope. P1 polish.
11. **`pnpm.onlyBuiltDependencies` allowlist** not committed in Remotion `package.json` — would silence the `pnpm approve-builds` interactive prompt on fresh installs.
12. **`--color-fg-faint` (2.10:1) audit** — confirm exclusively decorative usage (SVG strokes, separator dots, chart fills). If any text lands on this token, bump.
13. **Newsletter form mail-provider wiring (P2.1)** — `NewsletterPending` model captures emails now; mail provider integration deferred per master plan. Buttondown or Substack candidates per master plan.

### Pre-existing — Verification Pass
Run a **full platform verification** using the Claude Chrome extension (`mcp__claude-in-chrome__navigate`, `computer`, `read_page`).

1. **Homepage Variant A** — confirm `Dateline` ribbon, hero 2-col (`HeroLead` + `HeroVisualisation`), `.secondary-grid` with 3 `SecondaryArticle` + `SignalsRail`, `MethodologyNotes` band with [1][2] notes referencing INSEE + Challenges/Forbes/Oxfam. Nav has 5 items (Dossiers / Signaux en Direct / Annuaire / Territoire / **Méthode**). **InteractiveStrip now reads** "12 819 déclarations au registre AGORA par 1 455 représentants d'intérêts (2018-2026)" (Session 51 reframe — was "12 819 actions ciblant l'Élysée depuis 2022"). CTA: "OUVRIR LE DASHBOARD →". `SignalsRail` lobby card reads "12 819 **déclarations AGORA** ciblant…" (Session 51).
2. **Homepage pauvreté map (Session 51 fixes)** — `HeroVisualisation` choropleth must show **darker = more poverty** (Seine-Saint-Denis / Nord / Pas-de-Calais dark; Bretagne / Savoie / Vendée pale). Vertical legend top = max value (dark). Horizontal legend below the map shows real min/max derived from data (`9,1 % → 77,3 %`), not the old hardcoded `−8,0 % → +22,6 %`. All 5 DOMs render with color: Mayotte darkest (77,3 %), Guyane dark rose (52,9 %), Réunion / Guadeloupe mid-rose, Martinique lighter.
3. **`/signaux?type=lobby` dashboard (Session 51)** — 5 sections: hero stat strip (2 747 représentants · 131 842 déclarations · 20 ministères · 2018-2026); FIG. 1 ministry table (20 rows, Économie 21 121 · 1 786 reps, Boury Tallon 2,5 %; Armées 7 · 3 reps, Entreprises Fluviales 71,4 %); FIG. 2 top 20 representants (BOURY TALLON 3 090 / 18 min., ANTHENOR 2 384, Google France 1 166); FIG. 3 top 20 domaines (Agriculture 35 950, Santé 18 282 after normalization); FIG. 4 timeline chart stacked by top 6 ministries + Autres; FIG. 5 méthodologie callout. `/signaux?type=conflit|porte|media|ecart|dissidence` still renders the old signal-card grid.
4. **`/dossiers/bilan-macron` (Session 51 corrections)** — 2 final data fixes: Gini now reads "Coefficient de Gini (niveau de vie) 0,292 (2017) → 0,294 (2022), source INSEE (enquêtes Revenus fiscaux et sociaux)" — was Eurostat tessi190 with wrong baseline. Hospital beds unified on DREES ER 2025: HERO shows "-45 500 / Fin 2013 → fin 2024 (DREES)"; HEALTHCARE_DATA shows "412 800 (fin 2013) → 367 300 (fin 2024), -45 500 lits (-11 %) sur 11 ans"; CONTRAST_ROWS shows "-45 500 lits fermés (2013-2024, DREES)".
5. **`/dossiers/medias` redesign (Session 50 addendum)** — new investigative hero + 8 sections with `SectionHeader` format, `MediaBoard` 10 cards 3-col, Republique État français shows `SERVICE PUBLIC · 100% controle`, no LARP badges, no hydration error in `ArcomSection` timeline.
6. **Mini-maps** — `HeroVisualisation` + `ChoroplethFigure` click should route to `/territoire/{code}` with the separator (Session 50 addendum bug fix).
7. **Déport banners on 11 minister profiles (Session 47)** — spot-check Lecornu / Chabaud / Barrot.
8. **Minister profiles with HATVP data (Session 46)** — 29 Lecornu II ministers populated (Nunez / Geffray / Caroit / Darmanin / Vautrin).
9. **Lobbyist profile (Session 52)** — `/profils/lobbyistes/502999626` (Boury Tallon), `/profils/lobbyistes/784668618` (MEDEF), `/profils/lobbyistes/497632109` (SUNROCK, no AGORA match → fallback notice). Hero metadata grid, 4-cell stat strip, FIG. 1-4 (ministry table / domain bars / timeline / 50-row sample), legacy `ActionLobbyiste` block shown only when old data exists.
10. **Historical rémunérations panel smoke checks (Session 54, superseded by Session 68 Documents UX)** — before the DI/DSP simplification, the remuneration panel was verified on legacy HATVP/declaration tabs. **Lecornu**: Actuel 112 884 € · Cumul 858 245 € · 9 postes (2 en fonction) · 2011-2026; FIG. 1 showed 2 current cards, FIG. 2 had 7 past rows, FIG. 3 chart spanned 16 years; no double-counting (positions sum = 858 245 ✓). **Blanc député**: fallback mode — Actuel 3 553 € · Cumul 568 814 € · 33 activités, no FIG. 1/2, FIG. 3 only. **Brun sénateur**: fallback — 87 777 € / 670 544 € / 10 activités. **Dati non-filer**: panel returned null and the parent empty state rendered.

### After Verification
- ~~**Commit Sessions 51-56 bundle**~~ → Sessions 51-55 landed in earlier commits (`077d748`, `0397472`, `70dba60`); Session 56 committed at HEAD. Bundle line preserved in case future sessions accumulate again. Session 54 adds: new aggregator `src/lib/remunerations.ts` (dedupe semantics + 90-day reporting-period heuristic + fallback mode for profiles without `PersonnalitePublique`); new component `src/components/gouvernement/remunerations-panel.tsx` (hero stat strip + FIG. 1/2/3 + participations + methodology footer); panel integration in `/profils/[slug]/page.tsx`, `/profils/deputes/[id]/page.tsx`, `/profils/senateurs/[id]/page.tsx`. No schema changes, no seeds. Session 53 adds: 3 new Prisma models (`LobbyisteDirigeant`, `LobbyisteDirigeantCarriere`, `LobbyistePosition`) + 2 enums + reverse relations; new ingestion script `scripts/ingest-rne-dirigeants.ts`; new `lobby-owner` signal type + collector; new aggregator `getLobbyisteOwnership`; 3 new sections on `/profils/lobbyistes/[id]` + reverse-view on ministre `LobbySection`; 4 editorial research-output JSON seed files (all `verifie=false`); extended `ingest-research-output.ts`; new UI component `src/components/signaux/lobbyiste-ownership.tsx`; new helper `matchLobbyOrg` (word-boundary cross-reference); timeline chart filing-lag overlay; extensive DESIGN.md + handoff.md + frontend.md updates. Plus Session 51/52 carryover (see above). Migration `20260423112256_add_lobbyiste_dirigeant_layer` included.
- ~~**Run RNE dirigeants ingestion for the top 10**~~ → **Done Session 59.** 8 personnes physiques upserted across 6 lobbies (Anthenor, Boury Tallon, Com'Publics, Communication & Institutions, Google France, Lysios). 0 matched to PersonnalitePublique (expected — these are private-sector execs, not current ministers). Surfaced a UX dedupe issue: same person can appear twice in FIG. 3 (RNE + RESEARCH) when `fonction` differs — see Session 59 entry. CNB/FNMF/MEDEF returned 0 dirigeants (associations without SIRENE-declared officer).
- **Validate 5 editorial dirigeant carrière entries** (Archambault, Stefanini, Chenut, Beaudet, Lamarque) — proposed UPDATE drafted Session 59 (`UPDATE "LobbyisteDirigeant" SET verifie = true WHERE source = 'RESEARCH' AND "nomNormalise" IN (5 names)` + matching `LobbyisteDirigeantCarriere` cascade — 5 dirigeants + 24 carriere rows). **Awaiting user confirmation** before run. Per-row counts: archambault 6 carriere · beaudet 4 · chenut 4 · lamarque 6 · stefanini 4. **Teyssier d'Orfeuil holds** (`verifie=false` stays) — "Conseil de Paris 11e" claim still unverified; `Depute` table has no entry, suggests municipal not national; UI already renders the explicit "(à vérifier — Conseil de Paris 11e arrondissement, pas député national)" disclaimer.
- **UX dedupe — Lysios FIG. 3 et al.** Same person rendering as 2 cards (e.g. Archambault Jean-Luc · "Président de SAS" RNE + Archambault jean-luc · "Fondateur et Président" RESEARCH) because the composite unique key includes `fonction`. Group `LobbyisteDirigeant` rows by `(nomNormalise, prenomNormalise)` in `getLobbyisteOwnership` and render one card per person with a `fonction[]` list inside. Should land before scaling RNE ingestion to all 2 747 representants (see next).
- **Scale the RNE ingestion** from top 10 to all 2 747 AGORA representants after the pilot data reads well. Run without `--siren` to process every Lobbyiste row with a non-null `siren` (~10 min at 1 req/s + spacing, assuming no new IP block).
- **Sigle / acronym ↔ expansion map** — `matchLobbyOrg` currently misses acronym ↔ expansion pairs: "MEDEF" in a minister's EntreeCarriere.organisation does NOT match `Lobbyiste.nom = "MOUVEMENT DES ENTREPRISES DE FRANCE"` because the acronym isn't a substring. Add a sigle column to `Lobbyiste` (or a side-table of alternate names) and check against it too. Would unlock MEDEF, FNMF, CNB, UIMM, etc.
- **Clients / mandants layer** — HATVP registre carries partial client declarations per `Lobbyiste` that we haven't modeled yet. For cabinets-de-conseil (Boury Tallon, Anthenor, Lysios, Com'Publics) this is arguably more load-bearing than ownership since the client drives the agenda. Natural next scope.
- **Apply same palette flip on /territoire choropleths**: Session 51's `computeFill` helper inverts `t` for `!higherIsBetter` indicators (pov, cho, det). Already automatic for any page using `FranceMap` — verify on `/territoire` (chômage and dette tabs) that darker = worse.
- **`/signaux/[id]` case pages**: biggest editorial unlock from the investigative register — per-signal detail page reusing Variant C primitives (`ClassificationBar`, `BriefingRow`, `LeadDossier`, `ChoroplethFigure`).
- **`/dossiers` index hub**: nav currently points `Dossiers` directly at `/dossiers/bilan-macron`. Build a proper index listing surviving dossiers (bilan-macron, medias, financement-politique).
- **Real `/methodologie/*` content**: 5-section placeholder from Session 50 — fill Sources / Signaux / Mise à jour / Correction / Charte éditoriale.
- **Phase 5 token sweep**: migrate `/profils/**`, `/signaux`, `/territoire/**`, `/votes/**` pages from `bureau-*` to `ink-*`/`fg-*`. `.severity-*` → `.sig-tag`. Delete `bureau-*` ramp + Instrument Serif + DM Sans imports once zero callsites. Note: Session 51's new `/signaux?type=lobby` dashboard already uses the new token set.
- **Link remaining deputeId**: Only 6/17 deputy-ministers have `PersonnalitePublique.deputeId` populated.
- **Seed historical governments**: Run `pnpm seed:gouvernement` + `generate-carriere` for Borne/Attal/Barnier (104 members, data files ready).
- **3 missing déports** (Session 47 gap): HATVP cited 14, registre shows 11. Candidates: Farandou (SNCF), Lescure (Natixis), Pégard (Le Point). Re-check `info.gouv.fr` weekly.
- **Add `deport` signal type** to `src/lib/signals.ts` so the 11 déports feed `/signaux` — severity `CRITIQUE` for `PROCEDURE_JUDICIAIRE`, `NOTABLE` for others.
- **Domain normalization polish for `/signaux?type=lobby`**: Session 51's `DOMAIN_SYNONYMS` map in `src/lib/lobby-overview.ts` covers ~70 synonyms. If the top-20 domaines table surfaces other obvious pairs, add them (the raw AGORA field has 7 731 distinct values).
- **Lobbyiste linking coverage**: Session 51's Top 20 representants table links to `/profils/lobbyistes/[id]` when `normalizeLobbyisteName()` matches. Session 52 reuses the same normalization on the profile page itself. Fuzzy matching (Levenshtein, etc.) deferred to a later pass — gives the most uplift for names that differ by acronym expansion (e.g. `FNMF` vs `Fédération Nationale de la Mutualité Française`).
- **Upgrade `/profils/lobbyistes` list page to Variant A**: the list (Session 39-era) still uses `bureau-*` tokens and `card-accent` rounded cards while the detail (Session 52) is Variant A. Unify when next touching that route; swap `_count.actions` (ActionLobbyiste) for an AGORA-backed count joined via `normalizeLobbyisteName`.

### Phase 9G (remaining)
Research agents for ~80 new historical government members, HATVP declarations for Lecornu II (when published).

| Document | Purpose |
|----------|---------|
| [`documentation/phases/phase9-plan.md`](phases/phase9-plan.md) | Full data model, source analysis, sub-phase breakdown |
| [`documentation/phases/PHASE-9-CHECKLISTS.md`](phases/PHASE-9-CHECKLISTS.md) | Acceptance criteria per sub-phase (9A → 9G) |
| [`documentation/phases/PHASE-9-WORKFLOW.md`](phases/PHASE-9-WORKFLOW.md) | Parallel execution, worktrees, agent prompts |

Sub-phases:
- ✅ **9A–9F** — Schema + Bayrou seed; HATVP XML ingestion; profile UI (`ProfileHero` + `ProfileTabs` + section components); AGORA lobby (131,842 `ActionLobby`, 20 ministry codes); career timeline (`generate-carriere.ts`); research-output JSON workflow + 38 files (474 career + 13 judicial events).
- ✅ **9G (president)** — President profile migrated; `/president` 308-redirects to `/profils/emmanuel-macron`; 4 president-specific section components; 6-tab layout.
- ✅ **9H** — Lecornu II rework (Session 34): Bayrou mandates closed, 37 members seeded, AGORA scaled to 20 ministry codes, full pipeline re-run.
- ✅ **Deep Investigation (Session 44)** — 12 parallel agents (~200 web searches), 1 new judicial event (Darmanin CJR), 24 conflict flags. UI gained `CareerSection` revolving-door badge, `ParliamentarySection` vote-contradiction alert, `LobbySection` "Ancien employeur" tag.
- ⬜ **9G (remaining)** — Run seed + generate-carriere + research agents for ~80 historical govt members; HATVP declarations for Lecornu II when published; link remaining 11 deputeId values.

---

## Recent Sessions

| Session | What Was Built |
|---------|----------------|
| **68** (May 7) | **HATVP Documents tab simplification — canonical DI + DSP profile surface.** User feedback on `/profils/gerald-darmanin?tab=documents`: the prior CV/pay view and PDF-faithful dossier were too complex for a general reader. (1) **New component** `src/components/gouvernement/hatvp-declarations-section.tsx`. Async server component using `getDossierData()` to fetch DI + DSP once, then render a clean two-part HATVP surface: header source chips, two summary cards (`DI — Déclaration d'intérêts`, `DSP — Déclaration de situation patrimoniale`), then detail panels. (2) **DI reading model**: top metrics show revenus publics déclarés, revenus non publics, mandats électifs, and participations financières (`Néant` when empty). Detail panels split rows into `Fonctions publiques et gouvernementales`, `Revenus non publics déclarés`, `Mandats électifs`, and `Autres intérêts déclarés`. Non-public rows are explicitly badged `Non public`; examples include Humensis/book income and publisher advances. Mandats électifs are not duplicated in the public-function list. (3) **DSP reading model**: top metrics show actifs, passif, patrimoine net, and populated-section count; details prioritize `Immeubles`, `Assurances vie`, `Comptes & épargne`, and `Passif`, with secondary non-empty sections collapsed. (4) **Profile wire-up**: `src/app/profils/[slug]/page.tsx` now imports `<HatvpDeclarationsSection>` instead of `<PublicCvPaySection>` for standard minister `?tab=documents`; tab label is `Déclarations HATVP`. President `?tab=declarations` now uses the same renderer where data exists. The documents tab hides `ProfileSignalBanner`, `DeportBanner`, and `TrajectoireHero` to keep the DI/DSP surface focused. (5) **Terminology fixes**: `src/components/gouvernement/interets-section.tsx` label for HATVP `PARTICIPATION` changed from "Participations financières" to "Organes dirigeants et organismes"; true financial participations are displayed separately from `DeclarationInteret.participations`. (6) **Darmanin verification**: route surfaces values matching the local HATVP PDFs and DB: public/government functions 870 923 €, non-public Humensis income 35 539 €, elected mandates 275 307 €, DSP assets 263 453 €, passif 185 632 €, net 77 821 €. (7) **Checks**: `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `curl -I http://localhost:3000/profils/gerald-darmanin?tab=documents`, desktop and mobile Playwright screenshots. (8) **Legacy note**: `<HatvpDossier>` remains a PDF-faithful/raw renderer from Session 65, but it is no longer the primary public profile UX. |
| **67** (May 7) | **Hybrid homepage + sitewide profile-photo backfill.** Two pieces of work, both shipped + verified live. (1) **Homepage hybrid** — Session 66's full-dashboard pivot read too operational for first-time visitors per user feedback. Reverted top half to Variant A editorial frame (Bilan Macron lead enquête + Pauvreté monétaire `<HeroVisualisation>` rail + 3 `<SecondaryArticle>` cards + `<SignalsRail>` non-sticky rail + `<InteractiveStrip>` AGORA + `<MethodologyNotes>` `[1][2]`), then a hairline divider with `◆ Tableau de bord civique` eyebrow, then the Session 66 dashboard sections kept below the fold (`<GouvernementBoard>` → `<VotesStructurants>` → `<HatvpScoreboard>`) → mon-depute CTA. Editorial entry first, civic dashboard second. `<HeroLead>` import restored; `LEAD`/`SECONDARY`/`METHODOLOGY_NOTES` constants restored from `077d748` (Session 51 commit). (2) **Profile-photo backfill** — closed a long-standing data gap. Pre-state: `Depute.photoUrl` 2 101/2 101 set but every URL 404 (legacy `/dyn/deputes/{id}/image` pattern broken since AN site rebuild); `Senateur.photoUrl` 1 943/1 943 set but every URL 404 (Sénat URL pattern fully gone — site rebuilt, no `/senateur/{id}.html` page); `PersonnalitePublique.photoUrl` 0/158 set despite 37 minister photos sitting on disk in `public/photos/ministres/` (the `ingest-photos.ts` Wave 6 script copied the files but never linked them back to the DB row); UI uniformly fell back to initials. (a) **`scripts/ingest-photos.ts` extended**: deputy URL switched to working `https://www2.assemblee-nationale.fr/static/tribun/17/photos/{numericId}.jpg` (strip `PA` prefix from `Depute.id`, current XVII° législature served at `www2` not `www`); `where: { photoUrl: null }` filter dropped on deputies so the 2 101 broken URLs all get overwritten; senator URLs cleared via `updateMany({where: { photoUrl: { not: null } }, data: { photoUrl: null }})` (let `Avatar` fall back to initials cleanly rather than serve broken `<img>` tags); new 4th step `linkMinisterPhotosToPersonnalites()` reads `public/photos/ministres/`, derives slug from each filename (direct match OR via `MINISTER_FILENAME_ALIASES` 12-entry map for HATVP-style underscored names like `buffet_francois_noel` → `francois-noel-buffet`, surname-only `marcangelli` → `laurent-marcangeli`, freeform `SAINT-MARTIN-Laurent-2021` → `laurent-saint-martin`, `PANOSYAN` → `astrid-panosyan-bouvet`, etc.), updates `PersonnalitePublique.photoUrl = "/photos/ministres/{encodeURIComponent(file)}"`. Idempotent. (b) **New `scripts/fetch-wikipedia-portraits.ts`**: targets `PersonnalitePublique` rows where `photoUrl IS NULL` (or `--all` to refresh). For each row, calls `fr.wikipedia.org/api/rest_v1/page/summary/{prenom_nom}` first, falls back to `en.wikipedia.org` on miss; downloads `thumbnail.source` to `public/photos/ministres/{slug}.{ext}`; updates the DB row; logs provenance to `documentation/photo-credits.json` (slug, source `wikipedia-fr`/`wikipedia-en`, pageUrl, imageUrl, thumbnailUrl, fetchedAt). Polite pacing 1 500 ms between requests. **Critical retry layer**: `upload.wikimedia.org` rate-limits at ~100 req/min — cold first run got 87 portraits then started failing on download; rewrote `downloadImage()` with 4× exponential backoff (2/4/8/16 s) on HTTP 429/5xx + caught network errors → second pass got 14 more, third pass got 25 more. Final yield: **126 portraits downloaded**, only **4 truly unmatched** (`jean-baptiste-lemoyne`, `marc-guerini`, `michel-fournier`, `salima-saa` — Wikipedia confirms no portrait exists). (c) **End state**: `Depute` 2 101/2 101 (100% rendering AN photos), `Senateur` 0/1 943 (graceful initials fallback), `PersonnalitePublique` **154/158 (97%)**, **Lecornu II 36/37** active ministers covered (Macron + every cabinet member except Michel Fournier SE Affaires rurales). (3) **Verification gates**. `pnpm exec tsc --noEmit` clean. Chrome MCP DOM probe: homepage `<GouvernementBoard>` renders 8 minister photos / 0 broken; `/profils/ministres` 36 photos / 0 broken / 2 `MF` initials-fallbacks (Michel Fournier × 2 cards); `/profils/deputes` 30 AN-served photos / 0 broken; `/profils/senateurs` 0 `<img>` / 29 initials-fallbacks (clean); `/profils/emmanuel-macron` hero serves `/photos/ministres/emmanuel-macron.jpg` (330 px Wikipedia thumb, `naturalWidth=330`). Console clean across all 4 routes. (4) **Files**. New: `scripts/fetch-wikipedia-portraits.ts`, `documentation/photo-credits.json` (126 entries). Modified: `src/app/page.tsx` (hybrid restructure), `scripts/ingest-photos.ts` (deputy URL fix + senator clear + minister linker + alias map), `package.json` (added `fetch:photos` script alias). Updated: `documentation/handoff.md` Critical Quirks deputy/senator photo line + this entry; `.claude/rules/ingestion.md` Wave 6 description. No schema changes, no new dependencies. (5) **Open follow-ups**. (a) **4 PP without Wikipedia portrait** — hand-source if visibility matters (Lemoyne junior/Guerini/Fournier/Saa). (b) **8 historical-govt minister files** still unmatched on disk (`Véronique LOUWAGIE ©C.Aubert-CD61.jpeg`, `juliette-meadel.webp`, `manuel-valls.png`, `marie-barsacq.jpg`, `mohamed_soilihi_thani.jpg`, `neuder.jpg`, `patrick-mignola.png`, `rebsamen.jpg`) — will auto-link once Phase 9G ships historical-govt seeds. (c) **`MediaProprietaire.photoUrl`** + **`Lobbyiste`** schema doesn't have a photoUrl field at all — out of scope this session. (d) **Wikipedia-sourced images carry CC-BY-SA attribution** — `documentation/photo-credits.json` is the manifest; if surfaces grow toward commercial use, add a per-image credit caption from this manifest. (e) **`scripts/ingest-photos.ts` is now Wave 6 of two parts**: the script handles deputy/senator/minister-file pipeline; `pnpm fetch:photos` (new alias) handles the Wikipedia layer. Re-run order: `ingest:photos` first (writes deputy URLs + clears senator + copies minister files + links by slug), `fetch:photos` second (Wikipedia ingestion targets remaining PP nulls). Both are idempotent; either can be re-run independently. (f) **Senator photo strategy**: 1 943 senators uniformly fall back to initials. To restore real photos at scale would need either Wikipedia coverage extension (most senators don't have a Wikipedia page) or the new senat.fr CDN pattern (not yet discovered). |
| **66** (May 7) | **Investigation-surface transformation — 7-phase frontend rewrite + post-QA hardening.** Pivot from "many datasets exposed" to a consistent investigation surface; same reading order on every profile, same vocabulary across the site. (1) **Phase 1 — Foundation primitives**. 4 new shared components + signal registry. `src/components/source-chip.tsx` (claim-level chip with `type ∈ "officiel"|"presse"|"calcul"` + free-text `basis` field — preserves provenance like `"Calcul Observatoire d'après HATVP + AN"` verbatim, never flatten); `src/components/signal-formula.tsx` (expandable `<details>` "Comment ce signal est calculé ?" reading from registry — formula + thresholds + universal caveat + methodology anchor); `src/components/data-freshness-badge.tsx` (deliberately NOT `LastVerifiedBadge` — "vérifié" is reserved for officially-verified facts; uses "Mise à jour" prefix, amber when > 90 days); `src/components/relations-grid.tsx` (skeleton: `groups: { label, items: { type, title, href, meta? }[] }[]`). `src/lib/signal-types.ts` extended with `SIGNAL_REGISTRY` — 7 typed entries (`conflit`, `porte`, `lobby`, `lobby-owner`, `media`, `ecart`, `dissidence`), each carrying `{label, formula, caveat, methodologyAnchor, thresholds: {critique, notable, informatif}}`. Universal caveat embedded on every entry: **"Un signal est un croisement de données, pas une preuve."** (2) **Phase 2 — Minister profile 5-section frame**. Tabs converged to **Résumé / Signaux / Chronologie / Relations / Documents** for non-president ministers. New `<ProfileSummary>` component for the Résumé tab (current role + portefeuille + status + signal-count quick-jump tiles + freshness). Cross-cutting: `lastUpdated` slot added to `ProfileHero` (renders `<DataFreshnessBadge>` in status row), `<SignalFormula>` wrapped per narrative in `ProfileSignalBanner`, `<SourceChip>` in `judiciaire-section.tsx` / `deport-section.tsx` / `souverainete-section.tsx` (replaced ad-hoc inline `<a>` source links). President variant kept specialized tabs (parcours / promesses / bilan / lobbying / declarations / judiciaire) since promesses + bilan are unique editorial features that don't fit the canonical frame. (3) **Phase 3 — Homepage "Current State" dashboard pivot**. `src/app/page.tsx` rewritten from Bilan-led editorial to civic control room. **First viewport (4 substantive sections + Dateline strip)**: `<GouvernementBoard>` (Président + PM + 6 ministers ranked by `MandatGouvernemental.rang`, each card showing active déport count + lobby exposure level via AGORA join) → Signaux (existing `SignalsRail` promoted to full-width band) → `InteractiveStrip` (Pression d'influence) → `<VotesStructurants>` (3 most recent VOTE_FINAL scrutins with pour/contre/abstention bars). **Below fold**: `<HatvpScoreboard>` (4 aggregate tiles) → Territoires (`HeroVisualisation` choropleth) → Dossiers (rotation, Bilan demoted to one of 4 cards) → Méthodologie + Corrections band. (4) **Phase 4 — Deputy + senator 5-section refactor**. Same canonical frame, type-specific labels — deputy tab 3 = "Activité" (votes thématiques + recent votes), senator tab 3 = "Mandats" (timeline). `lastUpdated={declarations[0]?.dateDepot ?? updatedAt}` plumbed to hero. (5) **Phase 5 — Search investigation cards**. `src/lib/search.ts` extended with `enrichSearchResults(results, topN=5)` — cache-ready helper joining computed-signal counts on top 5 only (FTS schema unchanged). New `getLobbyisteAgoraCounts(ids)` helper enriches lobbyiste search results via `Lobbyiste.nom → ActionLobby.representantNom` aggregation. Search result cards now render: entity-type badge | title | subtitle | severity badge | signal-type chips | "Investiguer →" CTA. (6) **Phase 6 — Parti profile vocabulary alignment**. `/profils/partis/[id]/page.tsx` reorganized with 5-section h2 markers (Résumé / Signaux / Documents / Chronologie / Relations) instead of tabs — page is short enough for single-scroll. CNCCFP `<SourceChip>` at top of Résumé. Signaux block is a placeholder explaining party-level signals are computed at the personnalité level — universal caveat appended. Lobbyiste profile intentionally kept on its existing Variant A editorial register per audit (FIG. 1–6 + ownership sections already mirror the 5-section reading order conceptually). (7) **Phase 7 — Trust layer**. New `/corrections/page.tsx` reading 60-day `IngestionLog` feed + RESOLVED `ErrorReport` entries (sanitized URLs). New `<StaleDataNotice>` component (amber banner when `derniereMaj > 90d`, wired on minister hero). `/methodologie` extended with 7 anchored signal sections (`#conflit`, `#porte`, `#lobby`, `#lobby-owner`, `#media`, `#ecart`, `#dissidence`) generated dynamically from `SIGNAL_REGISTRY` — SignalFormula's "Voir la méthodologie complète" link now lands on the right anchor. (8) **Post-QA hardening — 7 findings + 1 sidebar**. (a) **HTTP redirect layer**: tab redirects moved from in-page `redirect()` (which rendered as streamed meta-refresh and returned 200 OK on `curl -I`) to `next.config.ts` `redirects()` with HTTP 308. **Critical regex gotcha**: minister source must use `:slug((?!emmanuel-macron$)[^/]+)` — `:slug(.+)` matches greedily across `/` boundaries and preempts senator/deputy rules (e.g. `/profils/senateurs/21042A?tab=mandats` would be redirected to `?tab=relations` instead of the senator-specific `?tab=chronologie`). Saved as memory `feedback_nextconfig_redirect_segments.md`. In-page fallback switched to `permanentRedirect()` for defense in depth. (b) **Tab validation**: `VALID_DEPUTE_TABS` / `VALID_SENATEUR_TABS` `Set`s — invalid keys fall back to `resume` instead of rendering blank content. (c) **Search president fix**: `PRESIDENT_STATIC.entityId` was `"macron"` but `getSignals()` keys ministers by slug (`ministre:emmanuel-macron`); changed to `"emmanuel-macron"` so president search results carry signal chips. (d) **URL normalization** at submit-time (`safeSubmittedUrl()` in `/signaler-une-erreur/actions.ts`) and render-time (`safeUrl()` in `/corrections/page.tsx`) — allows `/...` paths (rejecting `//` to block protocol-relative) and `http(s)://` URLs only. (e) **Methodology Section 2 rewrite** to match registry (was 6 outdated types, now 7 with current formulas + universal caveat). (f) `<SignalFormula>` added to tab-local signal blocks: deputy Signaux Intérêts (`type=conflit`), senator Signaux Intérêts (`type=conflit`) + commission overlap (`type=lobby`); parti signaux block surfaces the universal caveat directly. (g) **Lint script** `next lint` → `eslint .` (Next 16 dropped `next lint`); user-side: ESLint 10 crashed against the Next 16 + TypeScript lint stack, pinned to `^9.x` and added a working flat `eslint.config.mjs`. Saved as memory `feedback_eslint_10_incompatible.md`. (h) `next.config.ts` `turbopack.root` + `outputFileTracingRoot` set; `factcheck-manifest.ts` validates draft filenames to clear the broad-pattern Turbopack warnings. (9) **Files**. New: `src/components/{source-chip,signal-formula,data-freshness-badge,relations-grid,stale-data-notice,gouvernement-board,votes-structurants,hatvp-scoreboard}.tsx`, `src/components/gouvernement/profile-summary.tsx`, `src/app/corrections/page.tsx`, `eslint.config.mjs`. Modified: `src/app/page.tsx` (full pivot), `src/app/profils/{[slug],deputes/[id],senateurs/[id],partis/[id]}/page.tsx` (5-section frame + redirects + tab validation), `src/app/methodologie/page.tsx` (registry-driven signal sections), `src/app/recherche/page.tsx` (signal-enriched cards), `src/app/signaler-une-erreur/actions.ts` (URL sanitizer), `src/lib/search.ts` (enrichSearchResults + getLobbyisteAgoraCounts), `src/lib/signal-types.ts` (SIGNAL_REGISTRY export), `src/components/profile-hero.tsx` (lastUpdated prop), `src/components/profile-signal-banner.tsx` (SignalFormula per narrative), `src/components/gouvernement/{judiciaire,deport,souverainete}-section.tsx` (SourceChip migration), `next.config.ts` (14 new tab redirect rules + workspace-root config), `package.json` (lint script + ESLint 9 pin). **Build clean throughout** — every phase gated by `pnpm build`. (10) **Memory saved**: 6 feedback rules across `~/.claude/projects/-Users-aydenmomika-data-gouv/memory/` — `feedback_verified_language.md`, `feedback_signal_caveats.md`, `feedback_source_provenance.md`, `feedback_incremental_refactors.md`, `feedback_eslint_10_incompatible.md`, `feedback_nextconfig_redirect_segments.md`. (11) **Open follow-ups** (not load-bearing): senator/parti search-result signal enrichment (would need new collectors keyed at those entity types — `getSignals()` currently emits only `depute|ministre|mediaOwner`); 75008-style postal-code fallback in search (would need `postal-resolver` integration); full lobbyiste 5-section restructure (deferred per audit — current Variant A editorial register already mirrors the frame); media-owner profile type (no schema model yet); newsletter mail-provider wiring (P2 per master plan). |
| **65** (May 2) | **Unified HATVP Dossier tab — DI + DSP rendered together in one PDF-faithful component, with patrimoine layer ingested from PDFs.** Three-step build for a raw PDF-faithful renderer, now retained as audit infrastructure and superseded for the public profile tab by Session 68. (1) **Phase 2 — Dossier-style DI render**: replaced the legacy HATVP presentation (one chart, one card grid, no attestation framing) with a numbered-section dossier mirroring the official HATVP PDF structure (§1° Activités professionnelles … §7° Mandats électifs). Year-by-year amounts surfaced from `RevenuDeclaration` (was: single `montant` per position in `InteretDeclare`); §3° Participations dirigeantes (Darmanin: 37 unpaid board seats) made visible — previously hidden because `montant = 0 €`. Attestation chain reproduced verbatim (déclaration déposée le … signature électronique HATVP). (2) **Phase 3 — DSP schema + ingest**: 2 new Prisma models via migration `20260502132628_add_declaration_patrimoine` — **`DeclarationPatrimoine`** (`id` = dossierId derived from PDF filename like `"dsp34805"` for idempotent re-ingest, `personnaliteId?` FK to `PersonnalitePublique`, `nomNormalise`/`prenomNormalise`, `organe?`/`dateDepot?`/`regimeMatrimonial?` all currently null because cover pages are image-rendered, `sourcePdfUrl`, `typeDeclaration` ∈ `{DSP, DSPM, DSPFM}`) + **`PatrimoineRow`** (`rubriqueNum` 1..12, `rubriqueTitre`, `ordre`, `isNeant` boolean, `description Json`, `valeur Json?` — JSON payload deliberate because the 12 PDF sections each have different column shapes). New ingest script `scripts/ingest-dsp-pdf.ts`: shells out to `pdftotext` (poppler 26, system binary) via `spawnSync` (NOT the unsafe shell-form variant — security hook would block); identifies person from page-2 running header `DSP/{NOM}-{Prenom}` not the cover page (which is image-rendered and empty in `pdftotext` output); per-section parsers (§1° Immeubles uses `-layout` mode for row alignment, §4°/§6°/§7°/§10°/§11° share `parseDescriptionBlocks`, §5° has its own Etablissement-keyed parser, §12° passif strips column-header bleed-throughs and signature-block leakage); idempotent upsert on `DeclarationPatrimoine.id` + `deleteMany + createMany` on rows; surname-prenom split falls back to filename when both header sides are uppercase (FERRARI-MARINA case). **Result: 44 declarations ingested, 1 013 PatrimoineRow, 43/44 linked to `PersonnalitePublique` (1 unmatched: `DE MONTCHALIN-Amélie` slug variation).** Verified Darmanin DSP against PDF row-for-row — Actifs 263 453 € · Passifs 185 632 € · Net 77 821 €; Lecornu 3 immeubles parse correctly (770k Maison + 150k Maison Nue-propriété + 100k Terrain). (3) **Phase 4 — UX redesign**: first cut shipped two stacked dossier articles (DI + DSP) with full repetition (~12 viewport scrolls). User flagged the density problem; redesigned as one `<HatvpDossier>` parent component. New file [`src/components/gouvernement/hatvp-dossier.tsx`](../src/components/gouvernement/hatvp-dossier.tsx); new data layer [`src/lib/dossier-data.ts`](../src/lib/dossier-data.ts) (`getDossierData()` does both DI+DSP fetches + computes synthesis numbers + per-section row counts in parallel); new title map [`src/lib/dossier-titles.ts`](../src/lib/dossier-titles.ts) (short editorial labels for the navy bands, full PDF titles in `title=` tooltip for a11y). Layout (top→bottom): identity strip (one shared header, both dossier IDs, both source PDF links) → synthesis hero (6 numbers in 2 columns: Rémunérations [Année N · Cumul · Période] + Patrimoine [Actifs · Passifs · Net]) → sticky chip-strip section nav (anchor-linked, per-section row counts, empty sections muted) → DI body (§1°-§7°) → DSP body (§1°-§12°) → single attestation footer. **Density rules**: heavy sections (>10 rows like §3° dirigeants) auto-collapse via native `<details>` with synthesis line ("37 mandats déclarés — aucun rémunéré ▾"); empty sections render as a single horizontal "Néant" chip strip per declaration (one row instead of 7+ full-width cards); multi-year DI positions fold year-by-year amounts into `<details>` showing total + breakdown on click. Per-section compact row layouts: §1° Immeubles one rich line + value column; §6° Comptes table-style with "Total liquidités" synthesis footer; §12° Passif one line per dette with `Emprunt / Restant (amber) / Mensualité`. **Result: tab DOM height 7 000 px → 2 889 px** (~3× compaction), 0 redundant headers/footers, all data still accessible via expansions. Old `declaration-dossier.tsx` + `declaration-dossier-dsp.tsx` deleted. (4) **Plan files**: `~/.claude/plans/jazzy-conjuring-bee.md` (DSP schema + ingest + render plan, approved). (5) **Schema state**: total models now **57** (was 55 — added `DeclarationPatrimoine` + `PatrimoineRow`). Reverse relation on `PersonnalitePublique`: `patrimoines DeclarationPatrimoine[]`. (6) **Critical quirks captured**: cover-page metadata (dateDepot, organe, regimeMatrimonial) currently null on all DSP rows because HATVP PDFs render the cover as an image — `pdftotext` returns empty for page 1. Page-2 running header is the authoritative person identifier. §1° Immeubles MUST use `pdftotext -layout` (not linear) because linear mode separates columns from rows when there are multiple properties (Lecornu's 3 properties came back column-by-column). Amount column order in §1° is `[Prix d'acquisition, Valeur vénale, Montant des travaux]`. (7) **Open follow-ups**: backfill DSP `dateDepot`/`organe`/`regimeMatrimonial` from the same person's `DeclarationInteret` (filed alongside, so dates match); link `de-montchalin-amelie` slug to its DSP record (1 unmatched); `Origine de propriété` field sometimes captures "Montant des travaux" due to layout-text column bleed in §1° (cosmetic); §3° board-seat synthesis line currently says "37 mandats — aucun rémunéré" but could include Lille Métropole cluster framing; Phase 4 future polish: Historique tab with DI/DSP/DIM/DSPM diff view (DSPM versions ingested but renderer shows most-recent only). (8) **Files** (not exhaustive): `prisma/schema.prisma` (+ 2 models, + reverse relation on `PersonnalitePublique`); `prisma/migrations/20260502132628_add_declaration_patrimoine/`; new `scripts/ingest-dsp-pdf.ts`; new `src/lib/dossier-data.ts`, `src/lib/dossier-titles.ts`; new `src/components/gouvernement/hatvp-dossier.tsx`; deleted `src/components/gouvernement/declaration-dossier.tsx` + `declaration-dossier-dsp.tsx`; modified `src/app/profils/[slug]/page.tsx` at the time for `<HatvpDossier>` (later replaced in public profile UX by Session 68 `<HatvpDeclarationsSection>`). Source PDFs at `documentation/HATVP-PDF/` (44 DSPs/DSPMs/DSPFMs + 49 DIs/DIMs covering Lecornu II ministers). |
| **64** (May 2) | **Pre-2027 launch wave shipped — P0.1 + P0.2 + P0.3 + P0.4 + P0.5 + polish wave + multi-circo disclaimer.** Implementation of Session 63's master + 5 subplans, dispatched as parallel subagents (Wave 1 = P0.1 + P0.4 + P0.5 in parallel; Wave 2 = P0.3; Wave 3 = P0.2; verification gate after each wave). Full execution plan at `~/.claude/plans/misty-swinging-ladybug.md`. **User decisions confirmed**: env-driven domain (`NEXT_PUBLIC_SITE_URL` with localhost fallback), augment-only AI scope (sourcing gate sacred, humans register in `Root.tsx`), parallel-where-independent sequencing. (1) **Schema**: 2 new Prisma models + 2 new enums via migration `20260502120101_add_error_report_and_newsletter_pending` — `ErrorReport` (cuid PK; `url`, `type` ErrorReportType{FACTUEL/SOURCE/TYPO/AUTRE}, `description`, `contact?`, `status` ErrorReportStatus{PENDING default/RESOLVED/REJECTED}, `resolutionNote?`, timestamps; composite `@@index([status, createdAt])` for moderation queue) + `NewsletterPending` (cuid PK; `email @unique`, `sourceSlug?`, `createdAt`; `@@index([createdAt])`). Plus SQL-only migration `20260502143648_add_remotion_reader_role` creating a SELECT-only Postgres role for cross-repo Remotion reads — idempotent via `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`. **schema-architect** subagent flagged that the existing project convention is `cuid()` not `uuid()` (verified across 30+ existing models) and applied that. (2) **P0.1 Legitimacy Stack** — `src/app/apropos/page.tsx` (8-section async Server Component mirroring `methodologie/page.tsx` structure, anchor IDs `#financement` + `#lanceurs-alerte`, ClassificationBar + Eyebrow primitives, minimal-disclosure copy until P1.3 funding stack lands); `src/app/signaler-une-erreur/{page,actions,merci/page}.tsx` (form auto-fills `?from=` query, `<select>` enum, server action `prisma.errorReport.create()` + audit log via `prisma.ingestionLog.create()` directly — `logIngestion` helper is script-context only); `src/components/json-ld.tsx` reusable component + Organization in root layout + Person on `/profils/[slug]` + Person on `/profils/deputes/[id]` + Person on `/profils/senateurs/[id]` (4 JSON-LD blocks total); 3 OG images (1200×630, copying `profils/deputes/[id]/opengraph-image.tsx` 230-LoC pattern with initials monogram, no remote `<img>`); 4 Twitter card routes (1200×600); `src/app/robots.ts` + `src/app/sitemap.ts` (`MetadataRoute` exports, env-driven hostname, 8 166 entries: 110 personnalites + 2 101 deputes + 1 943 senateurs + 3 883 lobbyistes + 101 départements + 11 dossiers + 13 static routes); root error boundaries (`not-found.tsx` editorial 404 with search bar, `loading.tsx` 3-line `.pulse` skeleton, `error.tsx` `"use client"` retry boundary); footer activated 5 placeholders (Politique de correction → `/methodologie#correction`, Charte éditoriale → `/methodologie#charte`, Signaler une erreur → `/signaler-une-erreur`, Lanceurs d'alerte → `/apropos#lanceurs-alerte`, Transparence financière → `/apropos#financement`); 2 placeholders remain (Newsletter — P2.1; Proposer une enquête — no target route planned). (3) **P0.5 `/mon-depute` rebrand** — `src/app/mon-depute/page.tsx` 3-state UX (no-`?cp=` prompt screen with 3 example postal codes / ambiguous-commune chooser / resolved-deputy card); `src/components/mon-depute/depute-card.tsx` async server component (queries: identity via `prisma.depute.findUnique`, vote count, ConflictSignal count, top-3 InteretDeclare via PersonnalitePublique link, top-3 recent votes with vote-tags labels, top-3 ConflictSignal entries — explicitly does NOT query `EvenementJudiciaire` since signal is the canonical contradiction surface for deputies, judicial events are out of scope for the share-card framing); `src/components/mon-depute/share-buttons.tsx` `"use client"` (5 buttons WhatsApp first then X / Mastodon prompt-for-instance / Email mailto / Copy with 2-second toast — all inline SVG no `lucide-react`); `/mon-depute/opengraph-image.tsx` resolves deputy via same `searchParams.{cp,commune}` flow as page (rose-tinted monogram when conflicts > 0); `twitter-image.tsx` 1200×600 mirror; `generateMetadata` with dynamic title/description + canonical URL; footer "Mon député" link in Accès column; quiet single-line homepage CTA below `MethodologyNotes` ("Trouver votre député → /mon-depute"); `next.config.ts` 308 redirect from existing `/votes/mon-depute` → `/mon-depute` (preserves any inbound links). (4) **P0.4 polish audit + batch 1 fixes** — `documentation/launch-polish-audit.md` 234 lines (per-route table covering 28 routes, triage 4 Major / 38 Minor / 17 Cosmetic, hot-route fix priority list); 6 cross-cutting fixes shipped: (a) `prefers-reduced-motion` global gate extended in `globals.css` to `.fade-up` / `.bar-fill` / `.live-dot` / `.live-dot-amber` / `.pulse-dot` / `.sigint-section::after` (was only `.pulse`); (b) `generateMetadata` exports added to 11 list/hub pages (`/profils/{deputes,senateurs,elus,lobbyistes,partis}`, `/territoire`, `/territoire/economie`, `/votes`, `/votes/mon-depute`, `/dossiers/bilan-macron`, `/patrimoine`); (c) `/profils/ministres` mobile spot-fix (`PresidentCard`/`PremierMinistreCard` `max-w-lg` → `max-w-full sm:max-w-lg` + `overflow-hidden` + responsive padding); (d) French typography sweep on 5 editorial files (~30 lines fixed: NNBSP U+202F before `: ; ! ?`, curly apostrophes U+2019, em-dashes — scoped to `/page.tsx`, `/methodologie/page.tsx`, `/apropos/page.tsx`, `/signaler-une-erreur/page.tsx`, `/mon-depute/page.tsx`; dossier section components NOT swept — defer to P1); (e) `/territoire/comparer` `MetricRow` B-better path `bg-teal/5` → `bg-amber/5` (matches dept-B accent); (f) `/signaux` LARP fix — `Dernière analyse : {fmtDate(new Date())}` (render-time fake) → real `prisma.ingestionLog.findFirst()` query in the page's `Promise.all`. (5) **P0.3 AI pipeline + schema sharing** (longest pole, cross-repo) — applied SQL migration creating `remotion_reader` role; created `/Users/aydenmomika/packages/viral-schemas/` (`package.json` + `tsconfig.json` ES2022 / module ESNext / declaration true; `src/{index,factcheck,composition-props}.ts` re-exporting 7 Zod schemas + 2 const exports — subplan said 8 schemas, actual file had 7 + 2 consts, preserved verbatim); added `viral-schemas` `file:`-protocol dep + `@prisma/client@^7` + `@anthropic-ai/sdk@latest` + `prisma@^7` (devDep) to `/Users/aydenmomika/remotion/remotion/package.json` + `pnpm.onlyBuiltDependencies` allowlist; symlinked `data-gouv/prisma/schema.prisma` → `remotion/remotion/prisma/schema.prisma`; created Remotion `prisma.config.ts` injecting `remotion_reader` `DATABASE_URL` (Prisma 7 dropped `datasources` constructor option, used `@prisma/adapter-pg` + `pg.Pool` mirroring data-gouv's pattern); Remotion `.env.local` (gitignored) with placeholder `ANTHROPIC_API_KEY`; new `remotion/remotion/src/lib/db.ts` named `prisma` export; new `remotion/remotion/src/lib/draft-llm.ts` (Anthropic Claude Opus 4.7 default with `--model` flag override, prompt caching `cache_control: { type: "ephemeral" }` on system block, Zod-validated output with up to 2 corrective retries on parse failure, SHA-256 idempotency cache at `src/data/drafts/.cache/<hash>.json`, JSONL cost log at `logs/llm-calls.jsonl`); replaced local schema imports across 7 Remotion compositions with `import { ... } from "viral-schemas"` (collapsed `src/lib/schemas.ts` to thin `export * from "viral-schemas"` for back-compat); 4 LLM-assisted providers in `scripts/generate-fact-check.ts`: `personnalite:<slug>` (PromiseFactCheck via `PersonnalitePublique` + current `MandatGouvernemental` `dateFin: null`), `judicial:<personnaliteId>` (ContrastFactCheck via `EvenementJudiciaire WHERE verifie: true` — non-negotiable per `gouvernement.md`, runtime invariant assertion catches missing filter), `lobby:<ministereCode>` (ContrastFactCheck via `ActionLobby` GROUP BY ministry / domain, top representants), `conflict:<personnaliteId>` (ContrastFactCheck via `ConflictSignal` joined with `MandatGouvernemental` + `InteretDeclare` — judgment call: subplan referenced `ConflictSignal.personnaliteId` but actual schema uses `deputeId` with no `verifie` column, bridged via `PersonnalitePublique.deputeId → ConflictSignal.deputeId`; trusts precomputed signal as authoritative since the model lacks a verified flag); `scripts/llm-cost-report.ts` reads JSONL log, sums spend per day (last 7 days + total), reports cache hit rate, alerts at €5/day; smoke test `scripts/smoke-prisma.ts` confirms `await prisma.personnalitePublique.count() = 110` from Remotion; **kohler regression**: identical pre-existing dead-link warnings (Mediapart + francetvinfo HEAD non-2xx), no NEW errors; `psql -U remotion_reader -d datagouv -c "SELECT COUNT(*) FROM \"PersonnalitePublique\""` returns 110, `INSERT` rejects with `permission denied`. **Sourcing gate untouched**. (6) **P0.2 `/v/[slug]` landing pages** (Wave 3, blocked on viral-schemas) — `src/lib/factcheck-manifest.ts` (`server-only` import; `getDraft(slug)` + `listDrafts()` + `deriveSlugFromFilename(filename)` + `isContrastDraft`/`isPromiseDraft` type guards + `getDraftTitle`/`getDraftSubtitle` helpers; `path.resolve(process.env.REMOTION_DRAFTS_DIR ?? "../remotion/remotion/src/data/drafts")` resolution; `React.cache` for per-request memoization; defensive Zod safeParse, gracefully skips invalid drafts); `src/app/v/[slug]/page.tsx` async Server Component (Eyebrow + dateline → hero h1 framingTitle → hero subtitle framingSubtitle → verdict block color-coded by label → two-side comparison for Contrast or quote block for Promise → additional context `<dl>` → TLDR italic serif → sources expanded with `SrcChip` per source → 3 CTAs: "Voir le profil complet" → `/profils/<personnaliteSlug>` (hidden when absent), embed CTA stub `<button title="...">Intégrer ce fact-check (à venir)</button>`, newsletter form Server Action upserting to `NewsletterPending` model, redirect to `/v/<slug>?newsletter=ok` → footer with /methodologie + /apropos + /signaler-une-erreur links; `revalidate = 3600`); `/v/[slug]/opengraph-image.tsx` + `twitter-image.tsx`; `src/app/v/page.tsx` index listing all parseable drafts with category filter chips reusing `<SecondaryArticle>`; `generateMetadata` per slug; sitemap extended to include `/v/[slug]` for all 5 derived slugs (no `_meta.published` filter yet since existing 5 drafts predate the convention). 5 derived slugs live: `lecornu-5pm` (FactCheck-Lecornu5PM), `dissolution-pari` (FactCheck-DissolutionPari), `oxfam-milliardaires`, `pedopsychiatrie`, `policier-deces`. Hidden CTAs when `personnaliteSlug` absent (none of the 5 existing drafts have it yet). PromiseFactCheck `framingTitle` fallback to `topic` + `getDraftSubtitle` truncates `promise.quote` (untested in production — all 5 existing drafts are Contrast format). (7) **Polish wave (a11y + JSON-LD type + LCP triage)** triggered by launch-gate Lighthouse — `src/app/profils/[slug]/page.tsx:156` `hasOccupation.@type` `"GovernmentOrganization"` → `"GovernmentPosition"` (Schema.org subtype Google Rich Results expects); `src/app/v/[slug]/page.tsx` `<dl>` structure fix — `<SrcChip>` was sibling to `<dd>` inside the `<dl>`, moved INSIDE `<dd>` (axe `definition-list` rule passes); embed stub `<button>` `aria-label` differed from visible text — dropped the aria-label, kept `title` (axe `label-content-name-mismatch` rule passes); `globals.css` color contrast — `--color-fg-dim` `#5a637a` → `#727d93` (3.19:1 → 4.63:1 against `#0a0f1a`), `--color-bureau-400` `#64748b` → `#7a8399` (4.03:1 → 5.05:1), `--color-fg-faint` `#3f4860` (2.10:1) intentionally NOT bumped since exclusively decorative (SVG strokes, separator dots, chart fills exempt from WCAG 1.4.3 text contrast); `target-size` 44×44 px — `min-h-[44px] min-w-[44px]` on `share-button.tsx` profile button (was `py-1 px-3` ≈ 26px), `.mobile-menu-btn` `40px` → `44px`, `france-map.tsx` close button + "Voir le tableau de bord →" text button; LCP triage on `/v/lecornu-5pm` (4.4s vs 3.6/4.1s on `/`/`/profils/[slug]`) — root cause: `getDraft()` step 3 O(n) full-scan with per-file Zod validation when slug doesn't filename-derive in step 1; documented in TODO comment + deferred (needs build-time slug→filename index + integration test coverage). (8) **Multi-circonscription disclaimer fix** (Chrome MCP verification turned up the bug) — `/mon-depute?cp=75008` was rendering 18 Paris députés but title pretended Michel Barnier was THE answer (metadata used `take: 1` → alphabetically-first deputy). Root cause: no commune→circonscription mapping in schema (`Depute.circonscription` exists as a number but isn't joined to commune codes — INSEE COG doesn't ship one). Fix: `generateMetadata` drops `take: 1` and branches on `deputes.length` (single → existing title, plural → `Vos députés — {cp} ({deputes.length} circonscriptions) | L'Observatoire` + plural description); page body prepends a disclaimer with link to `elections.interieur.gouv.fr` so user can identify their circonscription. All N cards still render — data accuracy preserved, framing made honest. Edits applied via Python (NNBSP characters in original prevented direct string match). (9) **Verification gates** — Wave 1 verifier ran 37 acceptance checks: PASS (build clean, `tsc --noEmit` clean, all routes 200, JSON-LD valid, OG/Twitter cards 200 PNG, `/votes/mon-depute` → `/mon-depute` 308, sitemap 8 166 URLs, footer placeholder count = 2, no `lucide-react` imports in new files, no `EvenementJudiciaire` queries without `verifie: true`, no LARP violations, no default `prisma` imports). Wave 2 verifier: `pnpm --filter viral-schemas build` clean, both consumers `tsc --noEmit` clean, kohler regression identical, smoke test 110 personnalites / 31 verified judicials / 131 842 lobby actions, `remotion_reader` rejects writes / allows reads. Wave 3 verifier: 5 `/v/<slug>` URLs return 200 with sources expanded, OG/Twitter PNGs 200, sitemap entries present. **Launch gate (8 e2e checks per master plan)**: 6/8 PASS, 1 deferred (LLM e2e pending real API key — code path executes up to the LLM call boundary), 1 PARTIAL (Lighthouse perf `/v/[slug]` 63 against strict ≥ 90 — known LCP issue, deferred; perf 80+ acceptable per master plan caveat; a11y closed by polish wave). (10) **Files (not exhaustive)**: 2 schema migrations + 1 SQL-only migration; 6 new routes + 5 new OG routes + 5 new Twitter card routes; new components `json-ld.tsx`, `mon-depute/depute-card.tsx`, `mon-depute/share-buttons.tsx`; new lib `factcheck-manifest.ts`; new package `/Users/aydenmomika/packages/viral-schemas/`; Remotion side `src/lib/{db,draft-llm}.ts` + `scripts/{smoke-prisma,llm-cost-report}.ts` + 4 new providers in `generate-fact-check.ts` + symlinked schema + `prisma.config.ts` + `.env.local` + `.gitignore`; modified `src/app/layout.tsx` (metadataBase, JSON-LD `<script>`, twitter metadata block, footer activations + 2 new Accès links), `src/app/page.tsx` (homepage CTA), `next.config.ts` (308 redirect), `src/app/sitemap.ts` (extended for `/v/<slug>` + `/mon-depute`), `src/app/methodologie/page.tsx` (added `id="correction"`/`id="charte"` anchors), `src/app/profils/{[slug],deputes/[id],senateurs/[id]}/page.tsx` (JSON-LD injections + `GovernmentPosition` fix), `src/app/{v/[slug]}/page.tsx` (`<dl>` fix + embed-button aria fix), `src/app/globals.css` (color tokens + reduced-motion gate + mobile-menu-btn 44px), `src/components/share-button.tsx` (44×44), `src/components/france-map.tsx` (44×44 close + Voir-tableau buttons), `src/components/mon-depute/depute-card.tsx` (queries Depute + InteretDeclare + voteRecord + scrutin tags + ConflictSignal), 11 list/hub pages (`generateMetadata`), 5 editorial files (typography), `/territoire/comparer/page.tsx` (amber accent), `/signaux/page.tsx` (real IngestionLog timestamp), `/profils/ministres/page.tsx` (mobile). (11) **Open follow-ups** (delta from Session 63): set `ANTHROPIC_API_KEY` to enable LLM e2e; rotate `remotion_reader` password before non-local use; build commune → circonscription mapping (the real fix for `/mon-depute` multi-circo communes — 8 affected); LCP rewrite on `/v/[slug]` (build-time slug→filename index, needs integration tests); future drafts should include `slug` + `_meta.published` + `personnaliteSlug` fields; add `/v` to `STATIC_ROUTES` once first new draft ships; track down untracked `scripts/ingest-dsp-pdf.ts` (TS error breaks `pnpm build`, predates this wave); manual Google Rich Results test post-deploy; live Lighthouse a11y re-run post-deploy; typography sweep on dossier section components (P1); commit `pnpm.onlyBuiltDependencies` allowlist in Remotion; `--color-fg-faint` decorative-only audit; newsletter mail-provider wiring (P2.1). |
| **63** (May 2) | **Pre-2027 civic-virality strategy plan + 5 P0 subplans (planning-only, no code shipped).** Synthesizes the full strategic conversation into a master plan + five executable P0 subplans, all stored at `~/.claude/plans/`. (1) **Master plan** (`ok-give-me-final-dazzling-wave.md`, 20 KB): rejects the "video > site" framing, frames the system as a flywheel — Video (discovery) ↔ /v/[slug] landing page (conversion) ↔ site polish + legitimacy stack (trust) ↔ Tier-1 press citations (legitimacy moat). Four tracks: **P0 pre-launch** (legitimacy, landing pages, AI pipeline, polish, mon député rebrand), **P1 launch window** (citation/embargo strategy, format diversification, distribution ops), **P2 post-launch** (newsletter, bot layer, embed widgets — deferred per user), **Q4 2026** (2027 election-debate live fact-check dashboard, brainstorm-now/build-Q4/ship-Q1-2027). User-confirmed cuts: **ElevenLabs voiceover** (sound-off scrolling + AI-slop trust hit) and **Bilan Macron landscape format** (no winning surface). User-confirmed deferrals to P2: newsletter, X/Bluesky/Mastodon bots, quiz/perpetual-counter formats. Strategic constraint: pre-2027 organic reach build window expires at L52-1 commercial-advertising blackout in November 2026. (2) **Two parallel Explore audit agents** (read-only, 2026-05-02) ground every subplan in current codebase state: site is **~70 % launch-ready** with solid editorial foundation (`/methodologie` real charter, `/profils/[slug]` 343 LoC comprehensive, footer LARP-discipline placeholders intentional); **legitimacy-stack gaps**: no `/apropos`, zero JSON-LD anywhere, OG images missing on `/profils/[slug]`/`/signaux`/`/dossiers/[slug]`, zero `twitter-image.tsx`, no `/signaler-une-erreur`, no `robots.txt`/`sitemap.ts`, no `not-found.tsx`/`loading.tsx`/`error.tsx`, mobile minimal (one `MobileNav` + one `sm:` breakpoint). **`/mon-territoire?cp=` already does postal-code lookup** with `resolvePostalCode()` — `/mon-depute` is rebrand + branded card + share-buttons, not a build from scratch. **Remotion AI pipeline is greenfield**: `scripts/generate-fact-check.ts` is a CLI skeleton with one hardcoded `kohler` provider; sourcing-gate works; 4 TODO providers commented out (`bercy-lobby`, `berge-parjure`, `jeanbrun-logement`, etc.); zero `@anthropic-ai/sdk`; zero `@prisma/client` in remotion repo; zero DB connection between repos; 8 Zod schemas in `remotion/remotion/src/lib/schemas.ts` not shared with data-gouv. **Per-video landing pages 100 % greenfield**: no `/v/`, `/video/`, `/extrait/` route. (3) **P0.1 Legitimacy stack** (`p0-1-legitimacy-stack.md`, 13 KB): 8 deliverables — `/apropos` page mirroring methodologie structure (Mission/Origine/Financement/Conflits/Indépendance/Equipe/Cite-cette-source); JSON-LD via `next/script` with typed Prisma data (Organization at root, Person + GovernmentPosition on profiles); per-route OG images on 3 missing routes copying the 230-LoC `profils/deputes/[id]/opengraph-image.tsx` pattern; Twitter cards (1200×600) on root + 3 routes; `/signaler-une-erreur` form with new `ErrorReport` Prisma model + audit log via `logIngestion()`; `robots.ts` + dynamic `sitemap.ts` (Next.js convention) covering ~28 routes + every `PersonnalitePublique`/`Depute`/`Senateur`/`Lobbyiste` slug; root `not-found.tsx`/`loading.tsx`/`error.tsx`; footer placeholder activation (5 of 9 `<span title="À venir">` → real `<Link>`, Newsletter stays placeholder until P2). (4) **P0.2 Landing pages `/v/[slug]`** (`p0-2-landing-pages.md`, 14 KB, **blocked by P0.3** for shared schema): single source of truth = same draft JSON that feeds Remotion (file-based manifest at `remotion/remotion/src/data/drafts/<slug>.json`, read directly via `path.resolve` since repos are siblings); HTML chart renderer mirroring Remotion compositions using existing investigative primitives (`BarRows`, `TimelineDots`, `SecondaryArticle`, `SrcChip`, `Footnote`); 11-block layout (Eyebrow → Hero → Verdict → Two-side comparison or Quote block → Additional context table → TLDR → Sources expanded with full URLs → 3 CTAs: profil-complet/embed-stub/newsletter-form → footer); per-page dynamic OG image; optional `/v` index page. New utility `src/lib/factcheck-manifest.ts` with `getDraft(slug)` + `listDrafts()` + `React.cache`. New `NewsletterPending` Prisma model so P0.2 newsletter form doesn't lose subscribers during P0→P2 gap. (5) **P0.3 AI pipeline + schema sharing** (`p0-3-ai-pipeline.md`, 16 KB, **longest pole**): default scope **augment, not replace** (sourcing gate stays sacred, LLM drafts, human registers in `Root.tsx`); shared package via `file:` protocol — new `/Users/aydenmomika/packages/viral-schemas/` with the 8 existing Zod schemas + `ContrastFactCheckProps`/`PromiseFactCheckProps`, both repos depend via `"viral-schemas": "file:../../packages/viral-schemas"`; **read-only Postgres role `remotion_reader`** with `SELECT`-only privileges (committed as a DB migration in data-gouv); symlinked `prisma/schema.prisma` from data-gouv → remotion + `pnpm prisma generate` in remotion; new `remotion/remotion/src/lib/db.ts` mirroring data-gouv's named-export pattern; new `remotion/remotion/src/lib/draft-llm.ts` — Anthropic SDK (`claude-opus-4-7` default) with prompt caching on system prompt (editorial charter + schemas), Zod-validated output, retry-on-parse-failure max 2× with corrective prompt, idempotent SHA-256 cache at `src/data/drafts/.cache/<hash>.json`; **4 TODO providers fleshed out**: `personnalite:<slug>` (PromiseFactCheck via `PersonnalitePublique` + `MandatGouvernemental`), `judicial:<id>` (ContrastFactCheck via `EvenementJudiciaire WHERE verifie = true` — non-negotiable per `gouvernement.md`), `lobby:<ministereCode>` (ContrastFactCheck via `ActionLobby` GROUP BY ministry), `conflict:<id>` (ContrastFactCheck via `ConflictSignal` cross-table). Cost monitor `scripts/llm-cost-report.ts` with daily threshold alert (€5/day default). Sourcing gate untouched. Hardcoded `kohler` provider regression must still pass. (6) **P0.4 Polish audit** (`p0-4-polish-audit.md`, 12 KB): not a build, an audit-then-fix exercise. Deliverable is `documentation/launch-polish-audit.md` with route-by-route table (Desktop/Mobile status, Lighthouse perf+a11y scores, issues, fix status) covering 26 active routes + new P0.1/P0.2/P0.5 routes. Per-route checklist: visual/typography (heading hierarchy, font/color tokens, French typesetting U+202F before `:;!?`), functionality (no broken hrefs, no dead `<span title="À venir">` after P0.1, French validation messages), data integrity (empty-state graceful, French numeric formatting via `format.ts`, French long-format dates), SEO (`generateMetadata` + canonical), accessibility (alt text, aria-labels, AA contrast 4.5:1, keyboard tab order, `prefers-reduced-motion`), performance. Mobile spot-fix on `/`, `/profils/[slug]`, `/dossiers/bilan-macron`, `/territoire/[departementCode]`, `/v/[slug]`, `/mon-depute` at 375×812 + 414×896. Lighthouse targets: ≥ 90 perf and ≥ 95 a11y/SEO/best-practices on `/`, `/profils/[slug]`, `/v/[slug]` strict; aspirational elsewhere. `linkinator` crawl for dead-link sweep. axe DevTools for accessibility. Triage Major (blocks launch) / Minor (visible) / Cosmetic (post-launch ok), ship Major + Minor on hot routes. (7) **P0.5 Mon député rebrand** (`p0-5-mon-depute.md`, 13 KB): **deliberately narrow** — `/mon-territoire` stays as comprehensive territorial dashboard (13 data fetches, dense), `/mon-depute` is the WhatsApp/share-optimized personal-angle deputy-only experience. New route `src/app/mon-depute/page.tsx` accepts `?cp=`, 3-state UX (no-`?cp`-prompt-screen / ambiguous-commune-chooser / resolved-deputy-card). Branded result card `src/components/mon-depute/depute-card.tsx`: VOTRE DÉPUTÉ · postal_code header + identity block (Avatar + name h1 `.hd` + party + groupe + circonscription) + 4 stat cards (participation %, votes count, conflits red badge if > 0, mandats antérieurs) + HATVP highlights (3 most recent declarations) + Votes notables (3 most controversial) + Contradictions (top `ConflictSignal` red-accented if `verifie = true`) + share strip + "Voir profil complet →" CTA to `/profils/deputes/[id]`. Share buttons (`"use client"`) in `src/components/mon-depute/share-buttons.tsx`: WhatsApp first on every breakpoint (`wa.me/?text=`), X (`x.com/intent/tweet`), Mastodon (instance prompt), Email (`mailto:`), Copy-link with toast. Pre-filled message register: sober/factual ("Voici mon député selon L'Observatoire : [name] ([party])."). Dynamic OG image at `src/app/mon-depute/opengraph-image.tsx` cached by `?cp=` (Next.js 1h revalidate), initials monogram (no remote photo per existing rule), TEAL accent bar. Twitter card 1200×600. Footer "Accès" column gets `Mon député` link; homepage CTA single-line below methodology notes. Default: do **not** add to nav (5-item rule from Session 50 + mobile constraint). (8) **Sequencing**: P0.1 + P0.4 + P0.5 independent (3 concurrent subplans); P0.3 must precede P0.2 (shared schema dependency); end-to-end gate = pick a HATVP entry → LLM draft → sourcing gate accepts → human registers → Remotion renders → `/v/<slug>` HTML mirrors → JSON-LD valid → OG/Twitter cards render → mobile spot-fix passes → Lighthouse ≥ 90/95. Recommended ship order: P0.1 first (foundational, lowest risk), P0.3 next (longest pole), P0.4 + P0.5 in parallel anytime, P0.2 last after P0.3 lands. (9) **Open decisions surfaced for user review** across all subplans: AI pipeline scope (augment default vs replace), launch target date (currently dependency-driven), LLM model (Opus 4.7 default vs Sonnet 4.6 cheaper), schema-package format (local-link `file:` default vs pnpm workspace at `~/`), per-video slug strategy (kebab-title default vs factcheck-id), `/mon-depute` route name (recommended vs `/mon-territoire` rebrand), `/apropos` copy disclosure level, draft format default (ContrastFactCheck safer default vs PromiseFactCheck higher engagement). None launch-blocking. (10) **Files**: 6 plan files written at `~/.claude/plans/` (master + 5 P0 subplans), 0 code/schema/migration changes in this repo. **No build, no migration, no `pnpm db:generate` required.** This session was scoped strictly to planning + audit synthesis; implementation begins next session against the approved plan files. (11) **Open follow-ups** (queued for next sessions): user confirms 2 critical defaults (AI pipeline scope, /apropos disclosure level) → ship P0.1 first. After P0.1 done, parallel P0.3 + P0.4 + P0.5; then P0.2; then **launch gate** (8-step end-to-end test) → P1 distribution ops. The 2027 election dashboard design doc target by 2026-08-31 is a separate side-track. |
| **62** (Apr 29) | **(planning-only header bump — not yet logged in this table; resumed and superseded by Session 63 on May 2.)** |
| **61** (Apr 28) | **Souveraineté économique — `/souverainete` dashboard + `AcquisitionEtrangere` model + 34-phase seed + minister profile cross-link.** New transparency dossier tracking French strategic-asset losses 2014-2026. (1) **Research pass**: 27 hand-curated, sourced cases dispatched as parallel research agents over three batches — including Alstom-GE 2015 (foundational case, Macron approved as Min. Économie under Hollande), Lafarge-Holcim 2015, Technip-FMC 2017, Morpho/Idemia → Advent 2017, Linxens → Tsinghua 2018 (IEF not triggered, Marleix critique), Manurhin → EDIC/UAE 2018, Essilor-Luxottica 2018, Sentryo → Cisco 2019, Souriau → Eaton 2019, Latécoère → Searchlight 2019, **Photonis Teledyne blocked 2020 → HLD 2021** (rare blocked-then-thrived, IPO Exosens 2024), Carrefour Couche-Tard withdrawn 2021 (souveraineté alimentaire invocation), Stellantis (PSA-FCA) 2021, Talend → Thoma Bravo 2021 (re-domiciliation IEF-bypass), Forvia/Faurecia-Hella 2022, Sigfox → UnaBiz 2022, Bolloré Africa → MSC 2022, Exxelia → HEICO 2023 (action de préférence golden share), Aubert & Duval 2023 (Airbus+Safran+Tikehau French rescue), Eutelsat-OneWeb 2023 → French recap June 2025, **Segault Flowserve blocked 2023 → Framatome 2025**, Believe → EQT/TCV 2024, Vivendi 4-way split 2024 (Canal+ → London, Havas → Amsterdam), Sanofi/Opella → CD&R 2025 (Doliprane), **Atos → Bull → État 2026** (closest call: Czech Křetínský and Anglo-American creditors nearly took French nuclear-deterrent supercomputers; 31 Mar 2026 closing), Soitec NSIG ancrage domestique 2016, SFR-Altice 2014 → Altice debt restructuring 2024 → Bouygues+Iliad+Orange exclusive negotiations Apr 2026. (2) **Schema**: 4 new enums (`CategorieAcquisition` 10v, `MesureEtat` 10v, `SecteurStrategique` 21v, `PaysAcquereur` 18v) + `AcquisitionEtrangere` model with self-FK `parentDealId` for multi-phase deals (Photonis Teledyne→HLD, Alstom→GE→Arabelle, Eutelsat fusion→recap, Segault Flowserve→Framatome, SFR Altice→créanciers→opérateurs FR — 7 parent-child pairs total). Migration `20260425112300_add_acquisitions_etrangeres`. (3) **Seed**: `scripts/seed-acquisitions-etrangeres.ts` — 34 typed entries (~1100 lines), idempotent on `(cibleNom, dateAnnonce, categorie)`, two-pass upsert (parents first, children resolve `parentSeedKey` → `parentDealId` via Map). Logs to `IngestionLog` via `logIngestion()`. Runs in 0.3s. (4) **Page**: `/souverainete` Server Component (`revalidate=86400`) with hero (eyebrow + serif h1 with italic accent + dek + 4-stat strip showing 13 cessions / 2 vétos IEF français / 4 sauvetages / 58 Md€ cumulé) + 6 figures (FIG. 1 trajectoires by categorie, FIG. 2 pays acquéreurs, FIG. 3 secteurs, FIG. 4 mesures publiques activées, FIG. 6 cadence annuelle inline-SVG, FIG. 5 full case table with parent-child indentation and source-URL links). Section components in `src/components/souverainete/`. **Crucial counter logic**: vétos français counter excludes EU competition vetoes (Alstom-Siemens 2019 doesn't count); cumulative cession value sums only `CESSION_ETRANGERE` rows (excludes fusion/scission to avoid double-counting Stellantis 38 Md€, Essilor 48 Md€, Technip 13 Md€). (5) **Aggregator**: `src/lib/souverainete-data.ts` → `getSouveraineteOverview()` (`React.cache`-wrapped, 1 query) + `getAcquisitionsByMinistre({ nom, prenom })` (post-fetch filter on small table — substring match against `normalizeName(prenom + " " + nom)` so it handles `"Bruno Le Maire (avec Sébastien Lecornu)"` and `"Antoine Armand → Éric Lombard (Économie) + Sébastien Lecornu (Armées)"`). Label maps for all 4 enums exported here. (6) **Profile cross-link**: new `<SouveraineteSection nom prenom />` in `src/components/gouvernement/` follows the Phase 9 pattern (rose/teal/amber palette like `<DeportSection />`); returns `null` on no match. Mounted in `mandats` tab for ministers (after `<LobbySection />`) and `lobbying` tab for president. Verified live: **Bruno Le Maire → 8 dossiers** (Alstom-Siemens, EDF-Arabelle, Linxens, Latécoère, Photonis, Carrefour, Sigfox, Segault), **Macron → 2** (Alstom-GE, Technip-FMC), **Lecornu → 2** (Segault, Atos). (7) **Nav & footer**: `DossierNav` extended to 4 pills (`bilan-macron`, `medias`, `financement-politique`, `souverainete` — `souverainete` lives at top-level `/souverainete`, not `/dossiers/souverainete`). Refactored `DOSSIER_LINKS` to use explicit `href` field per item. `overflow-x-auto` added for narrow viewports. Footer "Accès" column got `Souveraineté économique` link. `<DossierNav currentSlug="souverainete" />` mounts on the page between hero and main. (8) **LARP discipline**: every row carries a real `sourcePrincipale` + `sourceUrl` + `sourceDate`; `verifie` flag tracked per row (33/34 verified, SFR→FR-operators 2026 left `verifie=false` since exclusivity ends 15 May 2026). No fake édition numbers, no fake bylines, no fake source counts. Methodology footer explicit on confidentiality of IEF decisions and incompleteness of corpus. (9) **Files**: 1 schema migration + 1 seed file + 1 data lib + 1 page + 4 dashboard section components (`souverainete-hero.tsx`, `souverainete-breakdowns.tsx` exporting 4 sub-sections, `souverainete-table.tsx`, `souverainete-timeline.tsx`) + 1 profile section (`gouvernement/souverainete-section.tsx`). 6 files modified: `prisma/schema.prisma`, `src/components/dossier-nav.tsx`, `src/app/profils/[slug]/page.tsx`, `src/app/layout.tsx`, `documentation/schema.md`, `documentation/handoff.md`. Tsc clean. **Build clean** end-to-end (5 routes verified HTTP 200: `/souverainete`, `/profils/bruno-le-maire?tab=mandats`, `/profils/emmanuel-macron?tab=lobbying`, `/profils/sebastien-lecornu?tab=mandats`, `/`). (10) **Open follow-ups**: (a) per-case detail page `/souverainete/[id]` not built — table is dense enough for v1, defer until needed. (b) ~10-15 more cases to round out corpus (Areva-Orano, Withings-Nokia, Sequans, GrandVision-via-Essilor, Vossloh-Cogifer, RTE/Mobility International, Sanofi vaccins-Pasteur). (c) `ministreReferent` is currently free-text — future migration could add nullable FK `ministreReferentId` to `PersonnalitePublique` for hard-link queries, but the substring-match approach scales fine on this dataset size. (d) world-map heat for `acquereurPays` distribution (the existing `<FranceMap>` doesn't fit since data is acquirer-country, not French territory). (e) homepage primitive surfacing one souverainete signal (e.g., "27 cas, 2 vétos depuis 2017 — voir le dossier") in `<InteractiveStrip>` rotation. |
| **60** (Apr 28) | **Signal banner — narrative-payload dedup + zero-AGORA noise filter + ÉCARTS/LOBBYING merge + seed-gouvernement auto-close generalization.** Closes the two open follow-ups from Session 57. (1) **Audit**: psql group-by on `MandatGouvernemental WHERE dateFin IS NULL` returned only Lecornu II (37 rows = 1 PRESIDENT + 1 PM + 19 ministre + 16 ministre délégué). Bayrou/Barnier/Attal/Borne/Lecornu I all properly closed. Session 57's ad-hoc patch held; no other historical cabinets leaked. (2) **`scripts/seed-gouvernement.ts` patch**: removed the hardcoded `DATE_FIN_BAYROU`/`GOUVERNEMENT_BAYROU` constants and the bespoke Bayrou-close block. Replaced with a generalized auto-close at the start of `main()`: derives the active config (the one with `dateFin === null` in the configs array), then `updateMany WHERE dateFin = null AND gouvernement != activeConfig.gouvernement`, setting `dateFin = activeConfig.dateDebut`. Throws on multiple active configs. Idempotent — `Closed 0 prior mandate(s)` on second run, confirmed in the live DB. Covers Bayrou (legacy, no config), gouvernement renames (Lecornu→Lecornu II), and any future reshuffle. The President is intentionally included: each config seeds a fresh Macron row, so the active config's seedGovernment call re-opens his mandate at `dateFin = null` after the auto-close runs. (3) **Banner narrative dedup** in `src/lib/signals.ts::upsert` — added `(type, headline)` per-person duplicate check. Severity + exposure still aggregate; only the narrative push is skipped. Surfaced on Bergé (one declared "Activité du conjoint : Victory" matched two `Lobbyiste` rows sharing nom="Victory" → 2 identical narratives) and Papin (one declared "CREDIT AGRICOLE" interest matched 3 sibling lobby rows → 3 identical narratives). (4) **`lobbyCompiled` sort** — `collectLobbyOwnerLinks` now sorts the compiled lobby array desc by AGORA count before iterating. When several lobbies share a searchCore, the most-active one wins the upsert headline-dedup race. Papin's surviving row is now CREDIT AGRICOLE ASSURANCES (26 AGORA filings) instead of the bare CREDIT AGRICOLE (0 filings). (5) **Zero-AGORA `interet_declare` filter** — early `return` in the interets `onMatch` callback when `stats.count === 0`. The HATVP lobbyiste table contains many small civic associations and municipal bodies that never file with AGORA — matching them on declared participations adds noise (Lecornu's "Syndicat Mixte de la Base Régionale de Plein Air et de Loisirs de Léry-Poses" gone). For `dirigeant_direct` / `ancien_dirigeant` / `carriere_prive` link kinds, even 0 AGORA is meaningful (former lobby exec → still relevant) — those branches keep their narratives. (6) **Cross-collector ÉCARTS+LOBBYING merge** — post-`Promise.all` pass before sort. When a person has both an `ecart` narrative and a `lobby` narrative AND their headlines lead with the same French-formatted number, the lobby narrative's `detail` (top representants) is appended to the ecart's detail, then the lobby narrative is removed from `narratives[]` and `types[]`. Both collectors read the same `actionLobby` count, so the leading number repeats verbatim — visually confusing in the À SURVEILLER strip. (7) **Cache key bump** `unified-signals-v1` → `v2` in `src/lib/signals.ts:794` to invalidate disk-persisted `unstable_cache` results from the old code path. (8) **Verified end-to-end** via curl + Chrome MCP DOM probe across 13 profiles after `pnpm exec tsc --noEmit` clean: **Lecornu** (PM, ratio 235:1) — was 3 narratives (ÉCARTS · LOBBYING · LIENS LOBBY Léry-Poses), now 1 merged ÉCARTS line "17 425 actions lobby contre 74 intérêts déclarés · Ratio 235:1 · Principaux représentants : BOURY TALLON & ASSOCIES, ANTHENOR PUBLIC AFFAIRS"; **Darmanin** (Justice) — 3→2 narratives (merged ÉCARTS + kept LIENS LOBBY Cardinal Liénart 158 AGORA); **Barrot** (Affaires étrangères, ratio 161:1) — 2→1; **Nuñez** (Intérieur, ratio 212:1) — 2→1; **Lescure** (Économie, ratio 2 112:1) — 3 narratives kept (PORTES TOURNANTES + LIENS LOBBY Natixis as carriere_prive + merged ÉCARTS); **Pégard** (Culture) — 3 narratives (PORTES + NEXUS MEDIAS + merged ÉCARTS 10 283:1); **Papin** (PME) — 4→2 narratives (PORTES TOURNANTES Système U/Auchan + ONE LIENS LOBBY CREDIT AGRICOLE ASSURANCES 26 AGORA); **Vautrin** (Armées) — unchanged at 1 narrative (LOBBYING-only, no ecart to merge); **Geffray** (Éducation) — unchanged at 1 narrative (ECART-only, no lobby to merge); **Bergé** (Égalité) — banner now empty (Victory had 0 AGORA → filtered correctly); **Fayssat** (deputy) — unchanged Conflits narrative; **/signaux** index + homepage HTTP 200, no error markers. (9) **Files**: `src/lib/signals.ts` (5 edits — upsert dedup, lobbyCompiled sort, interet_declare filter, cross-collector merge, cache-key bump), `scripts/seed-gouvernement.ts` (constant removal + main() generalization). No schema changes, no migration. **Build clean**. (10) **Open follow-ups**: (a) the cross-collector merge is currently bilateral (ecart × lobby only); if a future collector emits a third headline with the same leading number it'll surface as a duplicate again. Could generalize to "collapse all narratives sharing a leading number into the highest-severity one". (b) per-person dirigeant card collapse on `/profils/lobbyistes/[id]` (Session 59 carryover) — independent of this work. **(cont) Trajectoire hero — career swim-lane lifted out of the Parcours tab.** Editorial reframe: the profile is now identity-first, perspective-second. Reading order is hero (who they are now) → signal banner (what's flagged today) → **trajectoire (the whole career arc)** → tab content (the transcript). (11) **`career-section.tsx` refactor**: exported `LaneTimeline` + `detectReconduit`; removed the in-tab `<LaneTimeline />` invocation and the now-dead `withDates`/`minYear`/`maxYear`/`span`/`now` locals it depended on. The Parcours tab still renders the `SummaryStrip` + year-grouped `YearGroup` detail rows — the swim-lane is no longer duplicated. Single source of truth. (12) **`src/components/gouvernement/trajectoire-hero.tsx`** **NEW** (~55 lines) — async server component, prop `personnaliteId`. Queries `EntreeCarriere`, runs `mergeCareerEntries`, computes `minYear`/`maxYear`/`span`/`reconduit` (same logic the CareerSection used to do), renders the imported `LaneTimeline` inside a `rounded-sm border border-bureau-800/40 bg-bureau-900/30` hero card. Returns `null` if no entries OR no dated entries OR span ≤ 0 (sparse profiles fall back to tab-only). (13) **Page wire-up** in `src/app/profils/[slug]/page.tsx` — added import, rendered `<TrajectoireHero personnaliteId={personnalite.id} />` between the `<ProfileSignalBanner>` block and the `<div className="mx-auto max-w-6xl px-6 py-8">` tab-content wrapper. Visible on every tab without a click — it lives outside the tab-content div. (14) **Verified** via Chrome MCP layout audit on Lecornu (`/profils/sebastien-lecornu`): hero @ ~0–483 px → signal banner @ 483–602 → **trajectoire @ 602–795** (193 px tall, 5 lanes Gouvernement/Mandats électifs/Fonction publique/Formation/Autre, 18 segments spanning 2003–2026) → tab content @ 795+. Cross-profile segment counts via `href="#entry-"` anchor count: Macron 12, Lecornu 18, Darmanin 6, Bergé 11, Lescure 15, Barbut 15, Papin 13. All non-zero, none regressed. `pnpm exec tsc --noEmit` clean. (15) **Multi-role surfacing without special-case UI**: Lecornu (deputy → Armées → Outre-mer → PM) shows naturally as parallel bars across the Gouvernement + Mandats électifs + Fonction publique lanes — no perspective switcher needed yet. Ex-ministers (when seeded) get a Privé/Organismes bar after their Gouvernement bar ends — the revolving-door becomes visible as a *visual neighbor*, not a buried fact. (16) **Files**: `src/components/gouvernement/career-section.tsx` (export 2 helpers + remove invocation + dead-code cleanup), `src/components/gouvernement/trajectoire-hero.tsx` **NEW**, `src/app/profils/[slug]/page.tsx` (+1 import, +1 render). No schema changes, no migration. (17) **Open follow-ups**: (a) replicate `<TrajectoireHero>` on `/profils/deputes/[id]` and `/profils/senateurs/[id]` for unified-identity people — needs the same career data, which lives on `PersonnalitePublique.entreeCarriere`; the deputy/senator routes don't currently have a PP join. Smallest viable cut: add an optional join when `personnalitePublique.deputeId` or `senateurId` matches and pipe the PP id into the hero. (b) chip-perspective switcher above the hero (`MINISTRE · DÉPUTÉ · LOBBYISTE`) for multi-identity people — swaps the signal banner + active facts panel; trajectoire stays put. (c) eventually: 308-redirect `/profils/deputes/[id]` and `/profils/senateurs/[id]` to the canonical slug when the PP link exists, so one URL = one human. Earn each step by living with the previous one for a session. |
| **59** (Apr 26) | **RNE dirigeants pilot — top 10 lobbies ingested.** Closes Session 53's blocked pilot — IP rate-limit cleared. Ran `pnpm exec tsx scripts/ingest-rne-dirigeants.ts --siren 502999626 450089107 398995142 447525452 304426240 443061841 784668618 391576964 340388479 H771956256 --verbose`. Pre-flight `curl https://recherche-entreprises.api.gouv.fr/search?q=502999626&per_page=1` returned HTTP 200 in 110 ms confirming the ban had cleared. (1) **Results**: 10/10 lobbies queried, 6 returned dirigeants (CNB, FNMF, MEDEF returned 0 — expected since they're associations declared without an executive officer in SIRENE/RNE), 20 raw dirigeants found, 12 personne-morale skipped (e.g. holdings owning the lobby), **8 personnes physiques upserted** with `source = RECHERCHE_ENTREPRISES`, `verifie = true`. Run completed in 5.2s. (2) **Inserted dirigeants**: Anthenor — DE ROMANCE Timothé (DG) + LAMARQUE Gilles Paul (Pdt SAS); Boury Tallon — TALLON Pascal Claude Pierre (DG); Com'Publics — BERTOLINO Carine (commissaire aux comptes suppléant); Communication & Institutions — ALEXANDRE Fabrice (Pdt SAS); Google France — MANICLE Paul + YI Kenneth H. (gérants); Lysios — ARCHAMBAULT Jean Luc (Pdt SAS). (3) **0 matched to PersonnalitePublique** — expected: these are private-sector lobby executives, not currently serving ministers. The reverse-view "Dirigeant de lobby" red-accented block on minister profiles (Session 53 `LobbySection` addition) still empty across the 110 PP rows. Will populate naturally if/when a former lobby exec gets seated in a future government, OR when historical govt members are seeded with name overlap. (4) **Editorial + RNE coexistence verified** on Lysios (`/profils/lobbyistes/447525452`) — DOM probe shows FIG. 3 renders **3 dirigeant cards**: Archambault Jean Luc "PRÉSIDENT DE SAS · né(e) 1960 · RNE/API.GOUV" (new) + Archambault jean-luc "FONDATEUR ET PRÉSIDENT · RECHERCHE ÉDITORIALE · À VÉRIFIER" (Session 53 editorial seed) + Stefanini Patrick "Conseiller senior" (editorial). The two Archambault rows survive dedup because the composite unique key `(lobbyisteId, nomNormalise, prenomNormalise, fonction)` differs on `fonction` ("Président de SAS" vs "Fondateur et Président"). **UX side-effect**: same person renders as 2 cards on the lobbyist page. Worth a future per-person grouping in the UI (collapse cards by `(nomNormalise, prenomNormalise)`, show fonction list inside). Not blocking. (5) **Files**: zero code changes. Pure DB state change via `LobbyisteDirigeant` upserts. Idempotent — re-running won't duplicate. **Open follow-up scaling**: from top-10 → all 2 747 AGORA representants. Run without `--siren` to process every Lobbyiste row with non-null `siren` (~10 min at 1 req/s + spacing). Consider after the per-person UI dedupe is in place to avoid amplifying the visual duplicate. |
| **58** (Apr 26) | **Indemnité parlementaire extension — barème ordonnance 58-1210 on député + sénateur profiles.** Generalises Session 56's Indemnité officielle tile from ministers-only to all elected/appointed roles. (1) **`src/lib/baremes-officiels.ts` refactor**: `BaremeOfficiel` shape generalised — dropped `type: TypeMandat` field (unnecessary leakage), renamed `decretRefs` → `references` (since not all are décrets). Added `BAREME_DEPUTE` + `BAREME_SENATEUR` constants (both 7 637 €/mo brut → **91 649 €/an** brut, parity per ordonnance 58-1210, citing senat.fr/role/indemnite_senateur.html). New helper `getBaremeParlementaire(role: "depute" | "senateur"): BaremeOfficiel`. (2) **`RemunerationsPanel` refactor**: prop `mandatType?: TypeMandat | null` replaced by `bareme?: BaremeOfficiel | null` (pre-resolved by caller). The panel becomes purely presentational re: bareme — no enum knowledge, no lookup. Footer methodology paragraph updated: now reads "fixée par {references.join(' + ')}" instead of "le décret X et le décret Y" — handles "ordonnance 58-1210" without article-elision pain. (3) **Wire-up** (3 files): `/profils/[slug]/page.tsx` → `bareme={getBaremeOfficiel(activeMandat?.type)}`; `/profils/deputes/[id]/page.tsx` → `bareme={getBaremeParlementaire("depute")}`; `/profils/senateurs/[id]/page.tsx` → `bareme={getBaremeParlementaire("senateur")}`. Note: deputies and senators always show the bareme — the indemnité is statutory regardless of HATVP filing. (4) **Verified end-to-end** via Chrome MCP DOM inspection after `pnpm exec tsc --noEmit` clean: **Étienne Blanc** (`/profils/deputes/PA267075?tab=declarations`) → tile reads "INDEMNITÉ OFFICIELLE / 91 649 € / BRUT ANNUEL · DÉPUTÉ · ORDONNANCE 58-1210"; **Pierre Brun** (`/profils/senateurs/000018?tab=declarations`) → 91 649 € · SÉNATEUR · ORDONNANCE 58-1210, footer cites the ordonnance verbatim; **Lecornu** still renders 192 000 € · PREMIER MINISTRE · DÉCRET 2002-562 (Session 56 path unchanged). (5) **Closes Session 56 follow-up (a)**. **Open follow-up still**: parliamentary indemnité is indexed on the highest civil servant grade salary, not the indice point directly — the panel footer's "indexée sur le point d'indice de la fonction publique" is technically slightly imprecise for the parliamentary path. Acceptable editorially but worth a more precise wording when revisiting. (6) **Files**: `src/lib/baremes-officiels.ts` (refactor + new constants/helper), `src/components/gouvernement/remunerations-panel.tsx` (prop rename + reference rename), `src/app/profils/[slug]/page.tsx` (call site update + import), `src/app/profils/deputes/[id]/page.tsx` (call site + import), `src/app/profils/senateurs/[id]/page.tsx` (call site + import). No schema changes. **Build clean**. |
| **57** (Apr 26) | **Signal banner dedup — narrative.type + Lecornu I close-out.** Surfaced during a 10-scenario verification pass on top of Session 56. The "À SURVEILLER" block on every Lecornu II minister profile (Lecornu, Chabaud, Barrot, Vautrin, Bergé…) was rendering the lobby-écart payload twice with mismatched labels — e.g. row 1 "ÉCARTS HATVP / 17 425 actions lobby contre 74 intérêts déclarés · Ratio 235:1" + row 2 "LIENS LOBBY / 17 425 actions lobby contre 74 intérêts déclarés · Ratio 235:1" (same string, two badges). Two compounding bugs. (1) **UI bug** in `src/components/profile-signal-banner.tsx` line 55: `const type = match.types[Math.min(i, match.types.length - 1)]` — positional indexing into the deduplicated `types[]` array. When `narratives.length > types.length` (e.g. 3 narratives, 2 types), the surplus narratives all collapsed onto the LAST type. Fix: each `UnifiedSignalNarrative` already carries its own `type` field set by `upsert()` in `src/lib/signals.ts`; banner now reads `n.type` directly. Three lines deleted, one renamed. (2) **Data bug**: every Lecornu II minister had two active `MandatGouvernemental` rows — one for "Gouvernement Sébastien Lecornu" (the brief Sept 2025 first cabinet that fell + was re-formed days later) and one for "Gouvernement Sébastien Lecornu II", both with `dateDebut = 2025-10-12` and `dateFin = NULL`. The Lecornu I→II re-appointment seed never closed Lecornu I mandats. Confirmed via `SELECT gouvernement, COUNT(*) FROM "MandatGouvernemental" WHERE "dateFin" IS NULL GROUP BY gouvernement` → "Lecornu II 37" + "Lecornu 37". Effect: `collectDeclarationGaps` in `src/lib/signals.ts:381` iterates `mandatGouvernemental.findMany({where:{dateFin:null,ministereCode:{not:null}}})` and emits an écart per active mandat — every Lecornu II minister was double-emitted. Same effect leaked into anything else querying `dateFin=null` (Rémunérations panel `activeMandat?.type` lookup happened to return the same `type` for both rows so the bareme tile rendered correctly by luck). Fix: ad-hoc psql `UPDATE "MandatGouvernemental" SET "dateFin" = '2025-10-12'::date WHERE gouvernement = 'Gouvernement Sébastien Lecornu' AND "dateFin" IS NULL` — 37 rows updated, leaving 36 active mandats (Macron presidency + 35 Lecornu II ministers — Macron is `dateFin=NULL` PRESIDENT, plus there's a small mismatch worth a future audit). (3) **Cache trap**: `getSignals()` in `src/lib/signals.ts:795-797` is wrapped in `unstable_cache` with `revalidate: 600` (tag `"signals"`). Disk-persisted to `.next/cache/fetch-cache/`. After the data fix, simple dev-server restart wasn't enough — needed `rm -rf .next` to invalidate. Future: when you ad-hoc edit DB rows that feed `getSignals`, either bump the cache key (`"unified-signals-v1"` → `-v2`) or call `revalidateTag("signals")` from a server action, or wipe `.next/cache/`. (4) **Probe trace** revealed `unstable_cache` does NOT dedupe concurrent in-flight calls in dev — 3 concurrent `computeSignals` runs fired before the first finished (per `console.log` in `getSignals` + `computeSignals` enter logs). Each computed independently. `React.cache` correctly deduped within each render but not across the 3 dev-time renders Next.js 16 turbopack triggered. Not load-bearing for prod (cache settles on first render) but worth knowing if instrumentation shows weird call counts. (5) **Verified post-fix**: Lecornu now shows 3 distinct narratives (ÉCARTS HATVP · LOBBYING · LIENS LOBBY); Chabaud 2 (LIENS LOBBY Europe 1 + ÉCARTS HATVP 6 895/13/Ratio 530:1); Vautrin 1 (LOBBYING Armées 7 actions · MEDEF, EFF). No more cross-contamination. (6) **Open follow-up**: `scripts/seed-gouvernement.ts` should auto-close prior governments' mandats when a new one is seeded — currently it just inserts. Without this, the next government change will recreate the same duplicate. Also worth checking if any other historical governments left rows with `dateFin=NULL` (Bayrou, Attal, Borne, Barnier all properly closed per Session 34 rework — but verify before next reshuffle). (7) **Files**: `src/components/profile-signal-banner.tsx` (banner uses `n.type`); `documentation/handoff.md` (this entry). The DB UPDATE is non-codified — would need a one-shot script under `scripts/` if it had to be reproduced, but it's a state correction not a recurring transformation. Migration unnecessary. **Build**: `pnpm exec tsc --noEmit` clean. |
| **56** (Apr 25) | **Indemnité officielle (barème décret) on Rémunérations panel.** Reverses the Session 54 "HATVP-strict, no reference-salary constants" decision: when a person has an active `MandatGouvernemental`, the 3rd stat tile of the Rémunérations panel (was `POSTES 10 dont 3 en fonction`) now shows the public indemnité brute annuelle fixed by décret. Triggered by user observation that the `Actuel` figure on freshly-installed Lecornu II ministers is a 3-5 month period total mislabeled "par an" — the barème is more honest because it's a published value rather than a derivation. (1) **New module** `src/lib/baremes-officiels.ts` — typed lookup keyed on `TypeMandat` (Prisma enum, NOT `TypeMandatGouvernemental`). Values brutes mensuelles 2024-2026: PRESIDENT/PREMIER_MINISTRE 16 000 €/mo (192 000 €/an), MINISTRE/MINISTRE_DELEGUE 10 700 €/mo (128 400 €/an), SECRETAIRE_ETAT 10 200 €/mo (122 400 €/an). Each entry carries `decretRefs: ["décret 2002-562", "décret 2012-983"]` + `sourceUrl` (vie-publique fiche 19461). Helper `getBaremeOfficiel(type)` returns `BaremeOfficiel | null`. **Ministre and ministre délégué are paid identically** post-2012 reform — same value, two enum keys, intentional (see décret 2002-562 article 1 + 2012-983 -30 % uniform cut). (2) **Panel update** `src/components/gouvernement/remunerations-panel.tsx` — new optional prop `mandatType?: TypeMandat | null`. When set and `getBaremeOfficiel(mandatType)` returns non-null, the 3rd `StatTile` swaps from "Postes / N / dont X en fonction" to "Indemnité officielle / 192 000 € / brut annuel · Premier ministre · décret 2002-562". Footer methodology paragraph appends a sentence citing both décrets when bareme is active: "L'indemnité officielle (192 000 € brut annuel pour le Premier ministre) est fixée par le décret 2002-562 et le décret 2012-983, indexée sur le point d'indice de la fonction publique." Local helper `baremeArticle(label)` keeps the prose grammatical ("le Premier ministre" / "un ministre" / "un secrétaire d'État"). (3) **Wire-up** `src/app/profils/[slug]/page.tsx` — the page already computed `activeMandat = personnalite.mandats.find(m => m.dateFin === null)` for the hero; passes `mandatType={activeMandat?.type ?? null}` to `<RemunerationsPanel>`. **Deliberate scope choice**: only the active mandat triggers the bareme, NOT `currentMandat = activeMandat ?? lastMandatWithCode` — for ex-ministers no longer in office, "what they used to earn" would be misleading; POSTES tile remains. Deputies (`/profils/deputes/[id]`) and senators (`/profils/senateurs/[id]`) intentionally do not pass `mandatType` — they retain the existing POSTES/Activités tile (parliamentary indemnité not modeled yet, see open question below). (4) **Verified end-to-end** via Chrome MCP DOM inspection on 4 scenarios after `pnpm exec tsc --noEmit` clean: **Lecornu** (legacy minister HATVP surface) → tile read "INDEMNITÉ OFFICIELLE / 192 000 € / BRUT ANNUEL · PREMIER MINISTRE · DÉCRET 2002-562"; **Nuñez Intérieur** (legacy minister HATVP surface) → 128 400 € · MINISTRE; **Bergé Égalité** (legacy minister HATVP surface) → 128 400 € · MINISTRE DÉLÉGUÉ (same value as Nuñez, expected); **Étienne Blanc deputy** (`/profils/deputes/PA267075?tab=declarations`) → original "ACTIVITÉS / 33 / ACTIVITÉS DÉCLARÉES (AGRÉGÉES)" tile preserved (no `mandatType` passed). Footer methodology renders the décret citation correctly on all 3 minister profiles. (5) **Decision-log reversal**: Session 54 explicitly chose "HATVP-strict, no reference-salary constants, no monthly extrapolation" via AskUserQuestion. Session 56 flips this for the 3rd tile only — the rest of the panel (`Actuel`, `Cumul`, FIG. 1/2/3, footer) remains strictly HATVP-derived. The bareme is presented as a *separate* citable reference, not synthesized from declarations, so editorial integrity is preserved. The `Actuel` tile still reads "PAR AN (DÉCLARÉ)" so users can compare declared vs barème at a glance (Lecornu: 132 884 € declared vs 192 000 € barème — gap explained by partial-period filing). (6) **Source triangulation**: WebFetch struck out on 4 URLs (vie-publique direct, légifrance 403, lemonde paywall, info.gouv) before DuckDuckGo HTML returned a Wikipedia-cited synthesis confirming the 16 000 / 10 700 / 10 200 figures. Citations stored in module constants — when the indice point is reindexed (next planned: juillet 2026 increment), re-verify and bump. (7) **Files**: `src/lib/baremes-officiels.ts` **NEW** (76 lines), `src/components/gouvernement/remunerations-panel.tsx` (+import, +Props field, +bareme compute, +tile branch, +footer extension, +`baremeArticle` helper), `src/app/profils/[slug]/page.tsx` (+1 prop on the panel call site at line 265). No schema changes, no seeds, no migration. **Not committed** — bundles on top of Sessions 51/52/53/54/55 carryover. (8) **Open follow-ups**: (a) parliamentary indemnité barème for deputies/senators (≈ 89 916 €/an brut, ordonnance 58-1210) — would unify the 3rd tile across all profile types; (b) ex-ministers behaviour — currently POSTES; could show "Indemnité officielle (en fonction sous gouvernement X)" with disabled styling if useful; (c) the Wikipedia-cited 16 000 €/mo for President+PM groups them; if a more precise PR-specific figure is needed (often cited at ~15 200 € pre-Hollande adjustment), split the two enum entries. |
| **55** (Apr 24) | **HATVP source cache rename + `ingest-hatvp.ts` dedup fix.** Triggered by a 3-way audit (HATVP live PDFs from `hatvp.fr/fiche-nominative/` ↔ cached `declarations.xml` ↔ DB) on 10 random currently-serving Lecornu II ministers — 7/10 matched perfectly, 3 showed DB missing entries that were present in both PDF and XML. (1) **Root cause**: `scripts/ingest-hatvp.ts:596-607` dedup key was `(personnaliteId, declarationRef, rubrique, contenu)`. For declarants holding the same role over non-contiguous periods, all items shared `descriptionMandat` (e.g. Marina Ferrari had 3 separate "Députée" entries in `mandatElectifDto`: 2022-06 → 2024-02, 2024-07 → 2024-09, 2024-12 → 2025-10; Aurore Bergé had 2 "Députée des Yvelines" entries). They all collapsed to one row. Same issue on Vautrin's `participationDirigeantDto` + `participationFinanciereDto` cross-section duplicates. (2) **Fix**: added `dateDebut: interet.dateDebut` to the dedup `where` clause (single-line change). Re-ingest recovered **113 previously-collapsed rows** across 161 matched declarations. InteretDeclare: 1,988 → 2,102 rows (same 67 personnalites coverage). (3) **Source cache rename**: folder `documentation/hatvp-old-context/` → `documentation/source-cache/` (the old name implied stale; file inside IS the current Apr 21 merge). Deleted `declarations-2026-03-26.xml.bak` (143 MB, stale). Updated 6 scripts (`ingest-hatvp.ts`, `ingest-declarations.ts`, `ingest-deports.ts`, `ingest-scrutins.ts`, `ingest-organes.ts`, `audit-declarations.ts`) + `.gitignore`. Folder still holds mixed upstream caches (HATVP XML + AN organes/scrutins/deport JSONs + legacy blueprint/schema snapshots). (4) **Post-fix verification**: re-probed XML → Bergé has 3 mandats, Ferrari has 9, Vautrin has 28 participations — all 3 now match DB and live frontend (`/profils/{slug}?tab=documents` renders `Mandats électifs (3)`/`(9)` and `Participations financières (28)`). No schema change. Pattern caught: HATVP's XML represents each `<items>` block independently; any ingestion collapsing on `(contenu)` alone will lose non-contiguous same-title periods. (5) **Uncommitted** — bundles on top of Sessions 51/52/53/54 carryover. Files: `scripts/ingest-hatvp.ts` (1-line dedup fix + 4 comment lines) · 6 scripts with path sed (`source-cache` replace) · `.gitignore` (path update) · `documentation/source-cache/` (renamed, `.bak` deleted). |
| **54** (Apr 23) | **Rémunérations panel — HATVP public-office income aggregator on every profile.** Surfaces "how much does this official earn now / have they earned across their public career?" as the Session 54 remuneration surface in the legacy HATVP profile UX on ministres (`/profils/[slug]`), deputies (`/profils/deputes/[id]`) and senators (`/profils/senateurs/[id]`). Strictly HATVP data — no reference salaries, no monthly extrapolation. (1) **New aggregator** `src/lib/remunerations.ts` — `getRemunerations({nomNormalise, prenomNormalise, personnaliteId?})`, React.cache-wrapped. Merges `DeclarationInteret` (all `typeMandat`, no filter — **side-fix for multi-role officials** that the legacy `DeclarationSection` was missing) + `InteretDeclare` (rubriques `ACTIVITE_ANTERIEURE | MANDAT_ELECTIF | REVENU`) into a unified `RemunerationsData`. Dedupe semantics are explicit and load-bearing: positions keyed on `(contenu, organisation, dateDebut-month)`, latest `dateDeclaration` wins; `RevenuDeclaration` rows keyed on `(description, employeur, annee)`, latest depot wins. **Never mix the two for the lifetime total** — same money at different granularity would double-count. Returns `{currentPositions, pastPositions, yearlyBreakdown, lifetimeTotal, currentAnnualTotal, totalsFromYearly, currentAnnualYear, distinctActivitiesCount, participations, declarations, dataQualityNotes, latestDepotDate, firstYear, lastYear}`. (2) **HATVP reporting-period convention handled**: a position is "current" if `dateFin IS NULL` OR `dateFin >= today` OR `|dateDeclaration - dateFin| <= 90 days` — the third branch catches cases like Lecornu's 2026-02-27 DI where "Premier ministre" carries `dateFin 2026-02-01` as the reporting-period end, not the activity end. (3) **Fallback totals** for deputies/senators without a `PersonnalitePublique` row: when `currentPositions + pastPositions` are both empty but `yearlyBreakdown.length > 0`, totals come from summing `RevenuDeclaration` (`lifetimeTotal = sum of annual totals`, `currentAnnualTotal = latest year`, `distinctActivitiesCount = distinct (description, employeur)` pairs). Single data-quality note explains the fallback. (4) **New component** `src/components/gouvernement/remunerations-panel.tsx` — async Server Component with Variant A register: red `◆ Données HATVP · Rémunérations publiques` eyebrow + SrcChip linking to `hatvp.fr/{hatvpDossierId}`; 4-tile stat strip (Actuel / Cumul carrière publique / Postes (or Activités in fallback) / Période couverte) using `.obs-card` + `.hd` + `tabular-nums`; **FIG. 1 — En poste aujourd'hui** (signal-bg highlighted position cards with depot-date citation lines, rendered only when per-post data exists); **FIG. 2 — Postes passés** (4-col table Période·Poste·Organisation·Montant, sort desc by dateDebut, `<details>` collapse past 8 rows, `overflow-x-auto`); **FIG. 3 — Revenus déclarés par exercice** (inline-SVG stacked bars, rendered only when `yearlyBreakdown.length >= 2`; renumbered to FIG. 1 in fallback mode where it's the sole figure; TYPE_ORDER mapped to oklch tokens — `mandat_electif`=verified slate-blue, `professionnel`=#60a5fa, `consultant`=#c084fc, `dirigeant`=warn amber; 5 y-ticks + per-bar value labels + year ticks + legend dots); optional `Participations financières` card list; data-quality note list with `◇` warn markers; methodology footer with SrcChip citing dépôt range. Empty-state: panel returns `null` when all three lists + yearlyBreakdown + participations are empty (parent's existing "Aucune déclaration" empty state takes over). (5) **Integration** (3 files): `/profils/[slug]/page.tsx` inserts panel inside the then-active HATVP-tab block between the conflict-alert banner and `DeportSection`, above `InteretsSection` — editorial order alert→earnings→deport→rubriques; uses `personnalite.nomNormalise || normalizeName(personnalite.nom)` with `normalizeName` fallback for legacy rows. `/profils/deputes/[id]/page.tsx` + `/profils/senateurs/[id]/page.tsx` insert panel above `DeclarationSection` in `tab === "declarations"` with `personnaliteId={null}` — legacy `DeclarationSection` kept below as the per-depot raw drill-down. (6) **Verified end-to-end**: `pnpm exec tsc --noEmit` clean; `pnpm build` clean (33 routes); prod server curl + Chrome MCP DOM inspection on 4 scenarios: **Lecornu** (legacy minister HATVP surface) — Actuel **112 884 €** (PM 76 950 + commande d'ouvrage 35 934, both marked current via 90-day rule) / Cumul **858 245 €** / 9 postes dont 2 en fonction / 2011-2026; FIG. 1 shows 2 signal-bg cards; FIG. 2 has 7 rows (Ministre Armées 371 800, Min. Outre-mer 193 402, 5e VP dép Eure 72 079, Conseiller dép 57 194, Min. Cohésion territoires 50 886, Sénateur Eure "—", Adjoint maire Vernon "—"); FIG. 3 stacked chart 2011-2026; data-quality note "2 postes sans montant déclaré (exclus du cumul)"; **no double-counting** (lifetime total = sum of InteretDeclare only, confirmed manually: 76950+35934+371800+193402+72079+57194+50886 = 858 245); `<InteretsSection>` still renders below at y≈2591 (after `<DeportSection>` at y≈2280). **Étienne Blanc deputy** (`/profils/deputes/PA267075?tab=declarations`) — fallback mode, Actuel **3 553 €** (dernier exercice 2021) / Cumul **568 814 €** / 33 activités / 2015-2021; no FIG. 1/2 rendered (positions empty → suppressed); FIG. 3 chart renders from `RevenuDeclaration`; data-quality note explains fallback; legacy `DeclarationSection` accordion still renders below. **Pierre Brun senator** (`/profils/senateurs/000018?tab=declarations`) — Actuel 87 777 € / Cumul 670 544 € / 10 activités / 2017-2023, identical fallback pattern. **Rachida Dati non-filer** (legacy minister HATVP surface) — aggregator returns all-empty → panel returns `null` → parent's existing "Aucune déclaration d'intérêts publiée sur le registre HATVP" empty state renders untouched. (7) **Decision log**: user answered AskUserQuestion with scope=all-profiles, placement=top-of-HATVP-surface, sources=HATVP-strict (no reference-salary constants, no monthly extrapolation). The per-row `Dépôt HATVP {date}` citation + methodology footer + data-quality notes together make the panel auditable. (8) **Known limitation**: the 90-day reporting-period window is a heuristic — catches Lecornu's PM case but may not generalize to every HATVP convention edge. Revisit if a misclassification is spotted. (9) **Not committed** — bundled with Sessions 51/52/53 carryover. Files: `src/lib/remunerations.ts` **NEW**, `src/components/gouvernement/remunerations-panel.tsx` **NEW**, `src/app/profils/[slug]/page.tsx` (insert panel + normalizeName import), `src/app/profils/deputes/[id]/page.tsx` (insert panel above DeclarationSection), `src/app/profils/senateurs/[id]/page.tsx` (same). No schema changes, no seeds, no new dependencies. |
| **53** (Apr 23) | **Lobbyist ownership + government-ties transparency layer — top-10 pilot.** New 3-model schema (`LobbyisteDirigeant`, `LobbyisteDirigeantCarriere`, `LobbyistePosition`) + 2 enums (`SourceDirigeant`, `CategorieCarriereLobby`) + reverse relations on `Lobbyiste` and `PersonnalitePublique`. Migration `20260423112256_add_lobbyiste_dirigeant_layer`. (1) **New ingestion script** `scripts/ingest-rne-dirigeants.ts` hitting `recherche-entreprises.api.gouv.fr/search` (free, unauthenticated, 7 req/s). Auto-matches `dirigeants[]` (nom/prenoms/qualite/annee_de_naissance/nationalite) to `PersonnalitePublique` via `nomNormalise+prenomNormalise`. Serial CONCURRENCY=1 + 500ms pacing + exponential backoff up to 5×5s to avoid the IP bans that hit us during session 53's pilot attempts (the API temp-blocked the host after a burst; the script is correct, the IP just needs cooldown). Fallback by-name for HATVP-only ids like PLEAD's `H771956256`. (2) **Extended `scripts/ingest-research-output.ts`** with `--lobbyistes` / `--lobbyiste <id>` flags + new `data/research-output/lobbyistes/<id>.json` schema (dirigeants[] each with carriere[] + positions[]). Seeded 4 files with agent-researched editorial content, **all verifie=false pending manual validation**: `447525452.json` Lysios (Archambault + Stefanini — 10 carrière), `304426240.json` FNMF (Chenut + Beaudet→CESE — 8 carrière), `398995142.json` Com'Publics (Teyssier d'Orfeuil, flagged uncertain re "député" vs "conseiller de Paris" — 3 carrière), `450089107.json` Anthenor (Lamarque — 6 carrière). (3) **New `lobby-owner` signal type** in `src/lib/signals.ts::collectLobbyOwnerLinks()` and `src/lib/signal-types.ts`. Detects 3 link kinds: `dirigeant_direct` (current) / `ancien_dirigeant` / `carriere_prive` / `interet_declare` via freetext match (`EntreeCarriere.organisation` + `InteretDeclare.organisation` ILIKE Lobbyiste.nom core, word-boundary-aware). Severity `lobbyOwnerLinkSeverity()` keyed on lobby AGORA intensity × directness. **Works immediately without RNE ingestion** — surfaces 48 signals on first run from existing DB data: David Amiel/La Poste, Roland Lescure/Natixis, Jean-Pierre Farandou/SNCF, Monique Barbut/Air Liquide, Catherine Chabaud/Europe 1, Nicolas Forissier/Bpifrance, Laurent Panifous/Danone, Éléonore Caroit/Business France, Clara Chappaz/Google France. `/signaux?type=lobby-owner` filter wired. (4) **New matching helper** `matchLobbyOrg(lobbyCore, orgUpper)` in `src/lib/lobby-overview.ts` — word-boundary regex match in both directions (catches MEDEF↔"MOUVEMENT DES ENTREPRISES DE FRANCE" acronym expansion; rejects ORANGE↔ORANGERIE false positive). (5) **New aggregator `getLobbyisteOwnership(lobbyisteId)`** (React.cache-wrapped) returning `{dirigeants[], positions[], govTies[], counts}`. govTies are freetext-matched on the fly — no pre-computed table. (6) **`/profils/lobbyistes/[id]` gains 3 new sections** between existing FIG. 2 domains and FIG. 6 samples: **FIG. 3 Gouvernance** (dirigeant cards with collapsible carrière timeline, source badges, À VÉRIFIER warning for non-verified editorial claims), **FIG. 5 Recoupements** (gov-ties table linking to minister profiles with carrière/HATVP kind indicators), **Positions éditoriales** "Ce que X défend" (verifie=true filtered, empty-state "Couverture éditoriale en cours"). Dirigeants / govTies / positions render UNCONDITIONALLY — even lobbyists without AGORA match (like SUNROCK) show ownership block. (7) **Reverse view on `LobbySection`** (minister profile) — new "Dirigeant de lobby" red-accented block at top of LobbySection lists all `LobbyisteDirigeant` rows where `personnaliteId = this person`, with "En fonction"/"Ancien" badges + date range + link to `/profils/lobbyistes/[id]`. Currently empty for all ministers (0 RNE matches yet) — will populate once RNE ingestion succeeds. (8) **Defamation/privacy discipline**: `dateNaissanceAnnee` (year only, not full DOB), `nationalite` stored but never displayed individually, `verifie` defaults to false for editorial carrière/positions (mirrors `EvenementJudiciaire.verifie`), `sourceUrl + sourceDate` required on every entry. (9) **RBE bénéficiaires effectifs**: deliberately not modeled — locked by 2022 ECJ ruling, no public API as of April 2026. To be revisited if legitimate-interest access granted. Files: `prisma/schema.prisma` (+3 models, +2 enums, +3 reverse relations), `src/lib/signal-types.ts` (+`LobbyOwnerLinkSignal` interface + severity helper + `SIGNAL_TYPE_LABELS["lobby-owner"]`), `src/lib/signals.ts` (+collectLobbyOwnerLinks + wire), `src/lib/lobby-overview.ts` (export `normalizeLobbyisteName`, new `matchLobbyOrg`, new `getLobbyisteOwnership` + 4 new exported interfaces `LobbyOwnership` / `LobbyOwnerDirigeant` / `LobbyOwnerPosition` / `LobbyOwnerGovTie`), `src/app/signaux/page.tsx` (+"lobby-owner" in TYPE_ORDER + style maps), `src/app/profils/lobbyistes/[id]/page.tsx` (+3 new sections, renumbered FIG. labels), `src/components/signaux/lobbyiste-ownership.tsx` **NEW** (3 subcomponents), `src/components/gouvernement/lobby-section.tsx` (+LobbyRolesBlock reverse-view), `src/app/profils/deputes/page.tsx` + `src/components/profile-signal-banner.tsx` (type-map extensions for new signal type), `scripts/ingest-rne-dirigeants.ts` **NEW**, `scripts/ingest-research-output.ts` (+lobbyistes subdir support), `data/research-output/lobbyistes/{447525452,304426240,398995142,450089107}.json` **NEW**. Verified: `pnpm exec tsc --noEmit` clean; `pnpm build` clean (33 routes); dev server at :3000 returns 200 on all 6 pilot lobbyist URLs (447525452/398995142/304426240/450089107/502999626/443061841) + `/signaux?type=lobby-owner` + `/profils/david-amiel` + `/profils/clara-chappaz`. DOM spot-check: FIG. 1-6 all present on Lysios; "Qui dirige", "Liens avec le gouvernement", "Ce que … défend" all render; Boury Tallon (no editorial seed) shows all 3 empty-states correctly. Bug squashed: first version of `collectLobbyOwnerLinks` used plain `includes` substring match which false-positived on "ORANGE" vs "ORANGERIE" and similar; `matchLobbyOrg` uses `\b` word boundaries. **Not committed** — bundled with Session 51+52 carryover. **RNE API ingestion was attempted but the IP got temporarily blocked** (exit code 144 from `pkill`) — the script is verified correct via the editorial path; run `pnpm exec tsx scripts/ingest-rne-dirigeants.ts --siren 502999626 450089107 398995142 447525452 304426240 443061841 784668618 391576964 340388479 H771956256 --verbose` in a future session (likely needs ~1h IP cooldown, or new network). **(10) Timeline chart filing-lag fix** (triggered by "why is 2025 so low?" — raw data showed 2024 peak 26 050 → 2025 only 3 614 → 2026 only 101): `src/components/signaux/lobby-timeline-chart.tsx` now renders a diagonal-stripe "EN COURS DE DÉPÔT" overlay (SVG `<pattern id="in-progress-stripes">`) + amber dashed cutoff line + warn-colored year labels for years `> (currentYear - 2)`. `computeLastCompleteYear()` helper keys off HATVP art. 18-5 (3 months post-exercice-close filing deadline). Chart subtitle rewritten on both call-sites (`src/app/profils/lobbyistes/[id]/page.tsx` + `src/components/signaux/lobby-overview.tsx`) to cite the regulation explicitly instead of the earlier vague "derniers exercices peuvent être partiels". **(11) Chrome extension browser verification** — 9 URLs tested: Lysios (2 dirigeants Archambault+Stefanini with À VÉRIFIER badges + 10 carrière total + RECHERCHE ÉDITORIALE source chips), FNMF (Beaudet+Chenut, 8 carrière, Beaudet→CESE tracked), Anthenor (Lamarque, 6 carrière), Com'Publics (Teyssier d'Orfeuil with the "à vérifier — Conseil de Paris 11e arrondissement, pas député national" disclaimer rendering verbatim on the UI + "· non vérifié" markers per row), Boury Tallon (all 3 empty-states render correctly: "Aucun dirigeant" / "Aucun lien direct" / "Couverture éditoriale en cours"), **Google France (FIG. 5 surfaces Clara Chappaz "Manager chez Google" 2013→2018 via freetext match — first real cross-ref hit in the pilot)**, MEDEF (0 ties — documents acronym limitation), `/signaux?type=lobby-owner` ("Liens lobby (**40**)" pill active, down from 48 pre-`matchLobbyOrg` fix — 8 false positives removed including "ORANGE ↔ ORANGERIE"), `/profils/clara-chappaz?tab=mandats` (existing "Ancien employeur" amber tag on Google from Session 44 intact; new reverse-view block correctly absent because 0 `LobbyisteDirigeant.personnaliteId` matches — will populate when RNE ingestion runs). FIG. label renumbering verified: on Lysios, all 6 FIG labels present in DOM in order (1 ministères · 2 domaines · 3 dirigeants · 4 timeline · 5 liens gov · 6 samples). **(12) `documentation/DESIGN.md` updated**: frontmatter `description:` now mentions the ownership layer; 12 new YAML `components:` entries document the Session 53 UI tokens (`dirigeant-card`, `dirigeant-name`, `dirigeant-fonction-eyebrow`, `dirigeant-badge-{personnalite,source-rne,source-research,unverified}`, `ownership-empty-state`, `position-article`, `position-thematique`, `lobby-role-block`); new prose section "Ownership & government-ties layer (Session 53 — top-10 pilot)" covers models + enums, ingestion (structural RNE + editorial JSON), data flow (`getLobbyisteOwnership` + `matchLobbyOrg`), signals (`lobby-owner` severity rules), UI surface (FIG. 3, FIG. 5, positions with conditional numbering), reverse view, editorial/privacy rules (source priority, year-only DOB, no individual nationality, defamation guard, auto-match ambiguity, RBE deferred), pilot status April 23 2026, known limitations (acronym↔expansion gap, fuzzy matching deferred, clients/mandants as next wave). File grew 509 → 732 lines. |
| **52** (Apr 23) | **`/profils/lobbyistes/[id]` investigative profile upgrade.** Closed the loop between Session 51's dashboard and the individual lobbyist page. Before: the detail page was Session 39-era, used `bureau-*` tokens + `card-accent` rounded cards, displayed only the legacy `ActionLobbyiste` table (max 50 rows truncated), and had no connection to the 131 842-row `ActionLobby` AGORA dataset that the dashboard surfaces — so clicking a name in the Top 20 table took users to a sparse page that contradicted what the dashboard just showed. (1) **Data layer**: new `getLobbyisteAgoraDetail(name)` in `src/lib/lobby-overview.ts` (`React.cache`-wrapped). Resolves a `Lobbyiste.nom` to matching `ActionLobby.representantNom` values via the existing `normalizeLobbyisteName` (strips SAS/SA/SARL/SNC/EURL/GIE/SCOP/Association/Fédération suffixes) — same bridge Session 51 uses for the Top 20 → profile link. Returns `null` when no AGORA row matches. When matched, runs 5 parallel Prisma queries: `groupBy ministereCode`, `groupBy domaine`, `groupBy (exercice, ministereCode)`, `findMany take: 50` (samples), `mandatGouvernemental.findMany dateFin=null` (for current-minister links). Applies `DOMAIN_SYNONYMS` fusion so Santé/Système de santé/Agriculture/Agroalimentaire etc. collapse in the domain breakdown. Derives first/last year, top ministry, top domain, stacked-area timeline (top 6 ministries + `__other__`) from the groupBy results — no extra SQL roundtrip. Returns `LobbyisteAgoraDetail { matchedNames, totalDeclarations, distinctMinistries, distinctDomains, firstYear, lastYear, topMinistry, topDomain, byMinistry, byDomain, timeline, samples }`. (2) **Page rewrite** (`src/app/profils/lobbyistes/[id]/page.tsx`, 92 → ~550 lines): Variant A register throughout. Hero: back-link to `/signaux?type=lobby` + `◆ Registre HATVP · Lobbyiste` eyebrow, serif H1 (clamp 32→48px), optional catégorie dek, 6-field metadata grid (`type, SIREN, effectif, chiffreAffaires, adresse, dateInscription HATVP`), `SrcChip` with AGORA + HATVP sources, 4-cell stat strip (total déclarations + période, ministères ciblés + 1ᵉʳ label/count, domaines + 1ᵉʳ label/count, période `YYYY → YYYY`). Then 4 sections gated on `agora !== null`: **FIG. 1** ministry table (ministère / déclarations / part % / ministre en poste linking `/profils/[slug]?tab=mandats`); **FIG. 2** top-10 domain `BarRows` (signal/faded/faint palette ramp, same 3-break gradient used on the dashboard); **FIG. 3** stacked-area timeline via the existing `LobbyTimelineChart` reused from the dashboard — only renders when `timeline.years.length > 1`; **FIG. 4** 50-row sample table (exercice / ministère label / domaine / typeAction / tranche dépenses) with "X / Y lignes affichées" footer when `totalDeclarations > samples.length`. A separate **Registre HATVP** section renders the legacy `ActionLobbyiste` rows when `lobbyiste.actions.length > 0` — shown alongside the AGORA sections (MEDEF case) or alone (genuinely-legacy-only cases). When AGORA has no match, the hero drops the stat strip for a transparent notice: "Aucune déclaration AGORA appariée à ce nom. Le registre est réconcilié par normalisation (suffixes juridiques retirés) ; une absence peut signaler soit une inscription sans dépôts annuels, soit un libellé divergent côté AGORA." — don't fake a dashboard on empty data. (3) **Verified**: `pnpm exec tsc --noEmit` clean; `pnpm build` clean (33 routes); 3 HTTP 200 tests against real data — Boury Tallon (`502999626`, full AGORA dashboard with Travail · Santé · Agriculture · Économie surface correctly), SUNROCK (`497632109`, no AGORA match → fallback notice shown, FIG. 1-4 absent), MEDEF (`784668618`, both AGORA + legacy registre sections render). All inline label renders go through `ministryLabel()`; raw `MATIGNON`-style codes only appear in the RSC payload, not in visible DOM. (4) **Not committed** — bundled with Session 51 carryover. (5) **Next natural steps** (left for a follow-up): fuzzy name matching (Levenshtein/acronym expansion) for lobbyists whose AGORA `representantNom` differs by more than legal suffix; upgrade the `/profils/lobbyistes` list page (still on `bureau-*` theme) to Variant A and swap the `ActionLobbyiste` count for the AGORA-backed total. |
| **51** (Apr 23) | **Map palette fix + Bilan Macron final corrections + DOM poverty fallback + `/signaux?type=lobby` dashboard + Homepage InteractiveStrip reframe.** Seven interconnected workstreams. (1) **Bilan Macron final data fixes** (`src/data/bilan-macron.ts`): Gini coefficient switched from Eurostat tessi190 `28,8 (2017) → 30,4 (2024)` (wrong baseline — Eurostat gives 29,3 for 2017) to INSEE niveau de vie series `0,292 (2017) → 0,294 (2022)` with new source "INSEE (enquêtes Revenus fiscaux et sociaux)". Hospital beds unified on DREES ER 1225 (nov. 2025 publication) across 3 call-sites: HERO_STATS value `-45 500` / subtitle `Fin 2013 -> fin 2024 (DREES)` (was the unverifiable `-30 000 depuis 2017`); HEALTHCARE_DATA row `412 800 (fin 2013) → 367 300 (fin 2024), -45 500 lits (-11 %) sur 11 ans`; CONTRAST_ROWS `-45 500 lits fermés (2013-2024, DREES)`. These were the 2 corrections left outstanding from the Session 49 verification pass. (2) **FranceMap palette direction bug** (`src/components/france-map.tsx`): introduced `computeFill` helper that inverts `t → 1 - t` for `!higherIsBetter` indicators (pov, cho, det) so **darker = worse** by convention. Vertical legend gradient also flipped: top = max value = darkest for !higherIsBetter; unchanged for rev/pop/med. Previously pauvreté map showed lightest pink on max-poverty departments like Seine-Saint-Denis — counterintuitive. Fix propagates automatically to `/territoire` chômage + dette tabs. (3) **HeroVisualisation horizontal legend bug** (`src/components/investigative/hero-visualisation.tsx`): computed `min`/`max` internally from the `data` prop; dropped `legendLeft`/`legendRight` props from the interface. Homepage call in `src/app/page.tsx` was passing hardcoded `"− 8,0 %"` / `"+ 22,6 %"` which looked like a diverging delta scale — pauvreté is a monotonic rate (real range 9,1 % → 77,3 %). Horizontal gradient now derived from `INDICATOR_MAP[indicator].palette` with same flip logic as the vertical legend. (4) **Overseas DOM poverty fallback**: INSEE Mélodi FILOSOFI 2021 returns empty for `DEP-971`/`DEP-973`/`DEP-976` (confirmed via direct curl probe; only 972 and 974 have rows). Added `DOM_POVERTY_FALLBACK` constant in `scripts/ingest-insee-local.ts` upserted after the main loop + direct SQL upsert to populate DB immediately. Values: Guadeloupe 34,5 % (FILOSOFI 2017, INSEE Dossier Guadeloupe n°14), Guyane 52,9 % (FILOSOFI 2017, INSEE Insee Analyses Guyane n°45), Mayotte 77,3 % (Enquête BMF 2018, Insee Flash Mayotte n°142 — FILOSOFI excludes Mayotte). Result: all 5 DOMs now color on the map. Mayotte as 77,3 % pulls the global max up, so metropolitan departments compress to the lower half of the palette visually (honest tradeoff). Cache key bumped `france-map-data` → `france-map-data-v2` in `src/lib/france-map-data.ts` to force `unstable_cache` invalidation after the DB upsert. (5) **`/signaux?type=lobby` full dashboard rewrite** — replaced the 15-20 near-identical signal cards with a 5-section dashboard. Data-layer: new `src/lib/lobby-overview.ts` (5 React.cache-wrapped queries: `fetchStats`, `fetchByMinistry`, `fetchByRepresentant(20)`, `fetchByDomaine(20)`, `fetchTimeline(6)`) + `getPresidencyLobby` narrow query for the homepage + `DOMAIN_SYNONYMS` map (~70 entries covering Santé/Système de santé, Agriculture/Agroalimentaire, Transports/Logistique, etc.) + `normalizeLobbyisteName` helper (strips legal suffixes SAS/SA/SARL/SNC/EURL/GIE/SCOP/Association/Fédération for joining AGORA `representantNom` to `Lobbyiste.nom`). New `src/lib/ministry-labels.ts` extracted from `homepage-data.ts` with `MINISTRY_LABELS_LONG` + `_SHORT` + `ministryLabel(code, variant)` — 20 canonical codes including the 6 missing from the old map (`OUTREMER` without dash per AGORA, `ENSEIGNEMENT_SUPERIEUR`, `EDUCATION_NATIONALE`, `INDUSTRIE_NUMERIQUE`, `SPORTS_JEUNESSE`, `AMENAGEMENT_TERRITOIRE`). UI: new `src/components/signaux/lobby-overview.tsx` (5-section shell: hero stat strip, `Pression par ministère` 20-row table with concentration stat, `Top 20 représentants` table with category + ministries-touched + top 3 domains, `Top 20 domaines` with BarRows + detail cards, timeline chart, methodology callout) + `src/components/signaux/lobby-timeline-chart.tsx` (inline-SVG stacked area chart 2018→2026 × top 6 ministries + Autres). `src/app/signaux/page.tsx` gains early-return: `if (filterType === "lobby") return <LobbyOverview />`; other type filters still render the existing signal-card grid. (6) **Homepage `InteractiveStrip` reframe** (`src/app/page.tsx`): "Lobbying déclaré : **12 819 actions** ciblant l'Élysée depuis 2022" — factually wrong on both unit and date — replaced with "**12 819 déclarations** au registre AGORA par **1 455 représentants** d'intérêts (2018–2026)" + dek with top 4 named lobbyists (Boury Tallon 535, FNMF 240, Com'Publics 210, Anthenor 202) + "Ouvrir le dashboard →" CTA. Data flows via the new `getPresidencyLobby` slice in `getHomepageData()`. (7) **Signal headline language fix** (`src/lib/signals.ts::collectLobbyConcentration`): "actions de lobbying" → "déclarations AGORA" in the per-minister signal headline for language consistency. Also: (8) `src/app/globals.css` new responsive classes `.lobby-hero-stats` (1→2→4 col) + `.lobby-methodo-grid` (1 col mobile / 180px+1fr desktop). **Verified browser**: `/signaux?type=lobby` renders all 5 sections with real data (Économie 21 121 · Boury Tallon 2,5 % concentration; Agriculture 35 950 after domain normalization; timeline shows 2018 ramp + 2022-2024 plateau + 2025-2026 dropoff); `/signaux?type=conflit` still uses old signal grid; homepage strip shows new text + SignalsRail lobby card shows "déclarations AGORA". `pnpm exec tsc --noEmit` clean; `pnpm build` clean. Grep confirms no stale "depuis 2022" or "12 819 actions" strings remain in src/. |
| **50 (addendum)** (Apr 22) | **`/dossiers/medias` full redesign + MediaBoard rewrite + mini-map 404 fix + hydration fix.** Continuation of the Variant A sweep applied to the medias dossier, triggered by user feedback ("this looks very basic", "this tab is a mess and not aligned"). (1) **`/dossiers/medias` investigative overhaul** (`src/app/dossiers/medias/page.tsx`): complete rewrite of the JSX return. Added local `SectionHeader` helper matching Variant A register — eyebrow (`◆ KICKER` red) + optional `FIG. N` ratio label + `.hd` serif title + `.obs-serif` subtitle paragraph + optional `meta` slot. Replaced the old `DossierHero` call with an inline investigative hero block: `DOSSIER · MÉDIAS` / `MISE À JOUR` line → italic-third-line h1 "Concentration des médias : *neuf milliardaires,* 80% du paysage." → 2-sentence dek → `SrcChip` strip (SRC · ARCOM · HATVP · AGORA · Cour des comptes) → 4-col stat strip (**10** groupes cartographiés / **72** titres recensés · TV Radio Presse Numérique / **10** liens politiques déclarés / **20** signalements ARCOM · plus forte amende 3 000 000 €). 8 sections now unified via `SectionHeader`: **FIG. 1** — Cartographie du pouvoir médiatique (`PowerMap`) with side panel (Top 3 contrôlent 44% · HHI 1 134 concentré · 1/10 liens politiques · 1 groupe +3 ARCOM alertes + ranking list), **FIG. 2** — Propriétaires, fortunes, filiales (`MediaBoard`), **FIG. 3** — Régulation audiovisuelle (`ArcomSection`), **FIG. 4** — Connexions politiques (auto-fill minmax(320px,1fr) card grid of `contextePolitique` entries), **FIG. 5** — Lobbying Culture (2-col: BarRows représentants + BarRows domaines via `.lobby-culture-grid`), **FIG. 6** — Votes & lois culturelles (`TopicVoteList`), **FIG. 7** — Matrice titres × familles (`MediaTreemap`), **FIG. 8** — Intensité lobbying par ministère (`ConcentrationChart`). **LARP stripped**: no `SIGNAUX EN DIRECT` or `CELLULE D'INVESTIGATION` header, no context cards with fake source chips, no decorative descriptive block. All badges ("Confidentiel", "Incidents régulatoires", "Liens croisés", "AGORA", "Analyse structurelle", "Surveillance active") removed. (2) **`MediaBoard` rewrite** (`src/components/media-board.tsx`): replaced legacy `dossier-card` rounded-xl + `bureau-*`/`teal`/`amber` palette with Variant A `.media-card` rectangular tiles on ink-1 bg + `line` borders. Type colors remapped to CSS vars: TELEVISION/PRESSE_QUOTIDIENNE → `--color-verified`, RADIO → `--color-warn`, NUMERIQUE → `--color-signal`, PRESSE_MAGAZINE/AGENCE → fg-mute/fg-dim. Square outlined avatars (initials in cornered box) replaced round teal-tinted circles. Filter pills switched from teal gradient to rectangular mono-uppercase style with "FILTRER" label, `--signal-bg` active state. ARCOM/GOV badges use `.sig-tag` / `.sig-tag--amber`. **Fixed Republique alignment bug**: added `minHeight: 22` fortune-row slot; Republique État français now shows `SERVICE PUBLIC · 100% controle` placeholder (instead of collapsing the row and being shorter than other cards, which had caused the orphan card to break the visual rhythm). Type distribution bar is now a slim 3px track with oklch tokens. Expanded panel: `.media-card-expanded` with 2-col grid `minmax(240px,300px) 1fr` — owner dossier (formation/activity/control/fortune `<dl>` grid with obs-mono labels, political-context in signal-bg block, gov link as `.obs-mono` amber cta) + subsidiaries right-half (flex-col type groups with `.eyebrow` labels and obs-mono count, filiale pills as outlined chips with per-type oklch borders, orientation dot + ARCOM badge inline). New grid class `.media-board-grid` in globals.css (1-col → 2-col @640px → 3-col @1024px with `!important` to defeat inline base style). Added `.media-card:hover` border/bg lighten. Expanded-panel media query collapses to 1-col below 768px. (3) **PowerMap polish** (`src/components/power-map.tsx`): center node label switched from bureau blue/teal to serif `Source Serif 4` 28px for the total count + JetBrains Mono 8.5px `MÉDIAS RECENSÉS` tracking-0.18em. Added dashed inner red outline ring. Line contrast nudged from 0.15 → 0.28 (dashed for groups without `hasGovLink`, solid for linked). Hover glow filter applied to active node. Mobile responsive (container width observed via ResizeObserver, dimensions scale). (4) **ARCOM hydration fix** (`src/components/arcom-section.tsx`): swapped `new Date(s.date).getFullYear()` → `getUTCFullYear()` — the local-timezone version bucketed near-year-boundary dates into different years on the server (UTC) vs. the client (Paris), causing React hydration mismatches. (5) **Mini-map 404 fix**: `FranceMap.handlePathClick` builds URLs as `${linkBase}${code}` with no separator. `HeroVisualisation` and `ChoroplethFigure` were passing `linkBase="/territoire"` (no trailing slash) which produced `/territoire75` instead of `/territoire/75`. Added the trailing slash in both primitives. (6) **New responsive grid classes** added to `globals.css` for medias page: `.medias-hero-stats` (1→2→4 col), `.lobby-culture-grid` (1→2 col at md:), `.powermap-layout` (1 col → `minmax(0,1fr) 300px` at lg:), `.media-board-grid` (1→2→3 col), `.media-card` hover tint, `.media-card-expanded` mobile collapse. Verified: `pnpm build` clean, `pnpm exec tsc --noEmit` clean, page returns 200 at `/dossiers/medias`, DOM inspection confirmed 10 `.media-card` items render with new classes, `.sig-tag` badges render (14 ARCOM / 1 ARCOM / 2 ARCOM / GOV), expanded panel exists at 654px height with full owner-dossier + filiales content. Chrome extension went flaky mid-verification (screenshot timeouts) so final visual confirmation was via DOM queries rather than images — earlier screenshot showed 3-col grid with Republique orphan now aligned via `Service public` label. Still uncommitted (bundled with Sessions 44-50 carry-over). |
| **50** (Apr 22) | **Claude Design "Variant A" adoption (pivot from Variant C mid-session) — full design-system sweep foundations.** User initially chose Variant C ("Bureau intelligence briefing" — dense 3-col) then pivoted to **Variant A** ("Édition refined editorial" — single-hero 2-col + 3+rail grid + interactive strip + methodology footnotes) as the Variant C layout felt too cluttered. All Phase 0 token/font foundations from the Variant C round **kept** (they're palette-level, not layout-specific) — the pivot only swapped the homepage layout + added 6 Variant A-specific primitives. Final shipped stack:<br><br>**Phase 0 — Foundation (unchanged from C→A pivot)**: `globals.css` `@theme` extended with `--color-ink-0..3` dark ramp, `--color-fg*` text ramp, `--color-signal` (warm red oklch), `--color-verified` (slate-blue), `--color-warn` (warmer amber). Type tokens (`--fs-mono-xs/sm/body/lede`), hairlines (`--line`, `--line-2`). New utility classes: `.obs-mono`, `.obs-serif`, `.hd`, `.eyebrow` / `.eyebrow--red`, `.sig-tag` × 4, `.src-chip`, `.hair`, `.lnk-arrow`, `.rule-diamond`, `.pulse` (ring animation), `.obs-card`, `.obs-grain`, `.obs-footnote`. Fonts in `layout.tsx`: DM Sans → **Inter**, Instrument Serif → **Source Serif 4** (400/500/600 + italic), added **JetBrains Mono** as `--font-mono`. Old `bureau-*` + accents kept for backward-compat (progressive migration, no breakage).<br><br>**Phase 1 — Homepage (Variant A)**: `src/app/page.tsx` restructured — `Dateline` ribbon (pulse + "Signaux en direct" + date + real "Dernière ingestion · HH:MM UTC" from latest `IngestionLog.createdAt`) → Hero 2-col grid `.hero-grid` (`minmax(0,1.15fr) 340px` at lg:, 1-col below) with `HeroLead` (58px serif h1, italic third line, dek with `<Footnote>` [1][2] superscripts, SrcChip, read-link — no byline) + `HeroVisualisation` aside (pauvreté choropleth with caption + gradient legend + source) → `.secondary-grid` (3 `SecondaryArticle` + `SignalsRail` col-4 at lg:; 1/2/3+rail responsive; each SecondaryArticle has a Spark pulse preview) → `InteractiveStrip` (full-width bordered card, "Lobbying déclaré : **12 819** actions ciblant l'Élysée depuis 2022" — real PRESIDENCE AGORA count from `lobbyTop`; CTA → `/signaux?type=lobby`) → `MethodologyNotes` band (180px label col + 2-col notes, [1] INSEE ERFS + [2] Challenges/Forbes/Oxfam, both linking `/methodologie`). New data aggregator `src/lib/homepage-data.ts::getHomepageData()` — React.cache-wrapped, 6 parallel queries: `getSignals()`, `getTopLobbyTargets(6)` (new — AGORA groupBy with UPPERCASE_UNDERSCORE ministry-code label map), `getHATVPTimeline()` (new — raw SQL EXTRACT YEAR FROM DeclarationInteret.dateDepot + InteretDeclare.alerteConflit), `getIndicesSparklines()` (static src/data/indices.ts), `getDataHealth()` (IngestionLog.distinct source), `getFranceMapData()`. Variant A uses signals + mapData + revisionIso + one entry from lobbyTop; timeline/indices/dataHealth computed but currently unused (cheap, cached). Real lobby ranks: MIN ÉCO 21 121 / MIN TRAVAIL 18 460 / MATIGNON 17 425 / MIN TRANSITION 13 924 / **PRÉSIDENCE 12 819** / MIN CULTURE 10 283.<br><br>**Phase 2 — Primitives** (`src/components/investigative/`, 21 files total): **Variant A primitives** (used by homepage) — `dateline`, `hero-lead`, `hero-visualisation`, `secondary-article`, `interactive-strip`, `methodology-notes` + shared (`src-chip`, `read-link`, `spark`, `eyebrow`, `footnote`, `signals-rail`, `signal-card-c`). **Variant C primitives** (built first, retained for reuse on briefings / dashboards / `/signaux/[id]`, not rendered on homepage): `classification-bar`, `briefing-row`, `lead-dossier`, `secondary-dossier-card`, `bar-rows`, `timeline-dots`, `indices-panel`, `data-health-strip`, `choropleth-figure`. `/methodologie` reuses `ClassificationBar` + `Eyebrow` — validates cross-route reuse.<br><br>**Phase 3 — Chrome refresh** (`src/app/layout.tsx`): `ObservatoireLogo` (concentric circles crosshair, red outer + fg inner + red center dot, from chrome.jsx) replaces `EyeGlyph`. 5th nav item **Méthode** → `/methodologie`. Footer expanded from 3-col to **4-col** via new `.footer-grid` responsive class (1→2→4 col) — added CONTRIBUER column. Body/nav/footer migrated from `bureau-*` classes to `ink-*`/`fg-*` CSS variables (first reference file; rest of site still on bureau-*, migration is opt-in).<br><br>**Phase 4 — `/methodologie` placeholder** (`src/app/methodologie/page.tsx`): 5-section skeleton (Sources / Signaux / Mise à jour / Correction / Charte éditoriale) with `.hd` title + `Eyebrow` + body + "Page dédiée — À venir" tag. Uses `ClassificationBar` + `Eyebrow` primitives.<br><br>**LARP discipline maintained** (Session 48 rule still load-bearing): Variant A mockup had several fake signals we stripped — no `DOSSIER N°12`, no `LECTURE 18 MIN`, no `47 SOURCES`, no `PAR LA RÉDACTION · C. VOISIN / M. KEBIR` byline + avatar, no `FIG. 1/7`, no `EN DIRECT` wording (replaced with `Signaux en direct` eyebrow), no `MISE À JOUR AUTO`. `Dateline` shows real `IngestionLog.createdAt`; `InteractiveStrip` shows real AGORA count not mockup's decorative 1,289.<br><br>**Bugs fixed along the way**: (a) CSS comment containing `bureau-*/teal` prematurely closed the `/* */` block (replaced with "bureau and teal"); (b) Initial MINISTRY_LABELS map used kebab-case keys but AGORA uses UPPERCASE_UNDERSCORE (fixed to match real DB values).<br><br>**Verification** (Chrome extension was disconnected — HTML inspection instead): all 25 Variant A sentinels present (Dateline, hero kicker/date/h1/dek/sources/footnote-refs, vis caption, 3 secondary titles, signals rail, interactive-strip with real 12 819 count, methodology label, [1][2] note bodies, nav Méthode, footer Contribuer); all 11 LARP strings absent; 7 routes return 200 (/, /methodologie, /signaux, /profils, /territoire, /dossiers/bilan-macron, /profils/emmanuel-macron); `pnpm build` clean. Docs updated: `.claude/rules/frontend.md` (homepage section rewritten — Session 49 "decoration purge" retired; Variant A hierarchy documented; LARP discipline retained), `documentation/handoff.md` (this entry), MEMORY.md.<br><br>**Phase 5 (site-wide sweep — profiles/signaux/territoire to new tokens, delete bureau-*/DM Sans/Instrument Serif once zero callsites) deferred to follow-up PR.** All Sessions 44-50 still uncommitted. |
| **50 (initial — Variant C, superseded)** | **Claude Design "Variant C" adoption — full design-system sweep foundations.** User exported a Claude Design mockup (`/design-ref/` — variant-c.jsx + chrome.jsx + signals.jsx + viz.jsx + tokens.css) describing a dense 3-col "Bureau intelligence briefing" front page. User chose **high fidelity** (adopt mockup fonts + oklch palette) and **full design-system sweep**. 4 phases landed: (0) **Foundation**: extended `globals.css` `@theme` with `--color-ink-0..3` dark ramp, `--color-fg*` text ramp (fg/mute/dim/faint), `--color-signal` (warm red oklch), `--color-verified` (slate-blue), `--color-warn` (warmer amber). Added type tokens (`--fs-mono-xs/sm/body/lede`), hairlines (`--line`, `--line-2`), new utility classes (`.obs-mono`, `.obs-serif`, `.hd`, `.eyebrow`, `.sig-tag` + 4 variants, `.src-chip`, `.hair`, `.lnk-arrow`, `.rule-diamond`, `.pulse` with ring animation, `.obs-card`, `.obs-grain`, `.obs-footnote`). Swapped fonts in `layout.tsx`: DM Sans → **Inter**, Instrument Serif → **Source Serif 4** (400/500/600 + italic), added **JetBrains Mono** as `--font-mono`. bureau-* ramp + old accents kept for backward-compat (progressive migration, no breakage). (1) **Homepage rewrite** (`src/app/page.tsx`, was 283 lines → 230 lines of orchestration): replaced Session 48-49 2-col layout with `ClassificationBar` → `BriefingRow` → 3-col `minmax(0,1.4fr) minmax(0,1fr) 320px` grid (`.home-main` with responsive media queries — single-col below 1024px) → full-width `DataHealthStrip`. Col 1: `LeadDossier` (Bilan Macron, serif h1 via `.hd`, dek, SrcChip, "Mise à jour · Ouvrir le dossier →") + `<hr class="hair">` + `BarRows` (lobby top-6) + `<hr>` + 2× `SecondaryDossierCard`. Col 2: `ChoroplethFigure` (wraps `<FranceMap size="sm">` with pauvreté data) + HATVP TimelineDots (2017→2026, red dots for `alerteConflit=true`) + `IndicesPanel` (4-cell base-100=2017 with sparklines). Col 3: sticky `SignalsRail` with 6 compact `SignalCardC` items. New data aggregator `src/lib/homepage-data.ts` — single `getHomepageData()` runs 6 parallel queries: `getSignals()`, `getTopLobbyTargets(6)` (new — groupBy ActionLobby.ministereCode DESC), `getHATVPTimeline()` (new — raw SQL EXTRACT(YEAR FROM dateDepot) on DeclarationInteret + InteretDeclare.alerteConflit), `getIndicesSparklines()` (reads static `src/data/indices.ts` — 4 IndiceSpec with 12-point quarterly sparks, anchors from bilan-macron.ts), `getDataHealth()` (IngestionLog.distinct source, latest per), `getFranceMapData()`. React.cache-wrapped. Fixed ministry label map — AGORA uses UPPERCASE_UNDERSCORE codes (`ECONOMIE_FINANCES`, `TRAVAIL_SOLIDARITES`, `MATIGNON`, `TRANSITION_ECOLOGIQUE`, `PRESIDENCE`, `CULTURE`, etc.), not kebab-case. (2) **Primitives** — 15 new files in `src/components/investigative/`: classification-bar, briefing-row, lead-dossier, secondary-dossier-card, signals-rail, signal-card-c, src-chip, footnote, eyebrow, bar-rows, timeline-dots, indices-panel, spark, choropleth-figure, data-health-strip, read-link. All server components unless wrapping a client comp. (3) **Chrome refresh** (`src/app/layout.tsx`): replaced `EyeGlyph` with `ObservatoireLogo` (concentric circles crosshair — red outer + fg inner + red center dot, per chrome.jsx). Added 5th nav item **Méthode** → `/methodologie`. Footer expanded from 3-col to **4-col** (new `.footer-grid` class — responsive 1→2→4 col, added `CONTRIBUER` column). Migrated layout.tsx body/nav/footer from `bureau-*` classes to new `ink-*`/`fg-*` CSS variables (serves as pattern reference — rest of site uses bureau-* until Phase 5 sweep). (4) **`/methodologie` placeholder** (`src/app/methodologie/page.tsx`): 5-section skeleton (Sources/Signaux/Mise à jour/Correction/Charte éditoriale) with eyebrow + `.hd` title + body + "Page dédiée — À venir" tag per section. Uses `ClassificationBar` + `Eyebrow` primitives — first reuse validation. **LARP discipline maintained**: no ÉDITION 247, no "PAR LA RÉDACTION · C. VOISIN" byline, no DOS-12-2026 case-ID, no `47 SRC` chip, no `MISE À JOUR AUTO` badge (content is `revalidate=3600`, not streaming). Classification bar shows real `IngestionLog.createdAt`. **Verification** (HTML inspection — Chrome extension was disconnected): all 22 DOM sentinels present, all 5 LARP strings absent, 7 routes return 200 (/, /methodologie, /signaux, /profils, /territoire, /dossiers/bilan-macron, /profils/emmanuel-macron), 6 data-health cells render with `aria-label="ok"` status dots, lobby bars show live AGORA values (21 121 / 18 460 / 17 425 / 13 924 / 12 819 / 10 283 for top 6 ministries), briefing row shows real 30 critiques / 197 ouverts from `getSignals()`. `pnpm build` clean. Fixed one CSS parse bug (comment containing `bureau-*/teal` prematurely closed the `/* */` block — replaced with "bureau and teal"). Docs updated: `.claude/rules/frontend.md` (homepage section reversed — Session 49 "decoration purge" no longer applies; Session 50 register brings back chrome per Variant C intent; LARP discipline explicitly retained), handoff.md (this entry). **Phase 5 (full site-wide sweep — profiles/signaux/territoire pages to new tokens, delete bureau-*/DM Sans/Instrument Serif) is tracked as deferred follow-up PR.** Uncommitted. |
| **49** (Apr 22) | **Homepage visual polish + Bilan Macron full re-verification + Macron-era reframing** — Two distinct workstreams. (A) **Homepage layout & decoration cleanup** (`src/app/page.tsx` only, no schema/components): (1) Restructured the grid from `mx-auto max-w-7xl` + `lg:grid-cols-12` (which left a ~250px dead right gutter at 1728px viewport) to `lg:grid-cols-[minmax(0,1fr)_380px]` with `lg:pl-[max(1.5rem,calc((100vw-80rem)/2+1.5rem))] lg:pr-6`. Editorial column left edge now aligns with nav content edge, ticker pins to the right viewport edge with only a normal `pr-6` gutter. (2) Capped editorial column at `lg:max-w-[56rem]` (was unlimited / 1024px). (3) **Decoration purge** — typography now carries hierarchy: deleted the bordered `ProvenanceTag` pill component (replaced with plain "Sources : X · Y · Z" muted line), deleted "LIRE LE DOSSIER →" / "ANALYSER LE DOSSIER →" CTAs from secondary cards and ticker items (the entire card is already a `<Link>`), deleted the "MISE À JOUR AUTO" badge from ticker header (was misleading — page uses `revalidate=3600`, not live), deleted "Lecture : 18 min" tag (not meaningful for a data dossier), deleted `IconCommit` SVG, replaced "//" separator with `·`, dropped the teal-gradient bordered `InteractiveTeaser` card for a plain border-top horizontal row. (4) Lead H1 capped at `max-w-3xl` and reduced from `text-6xl` to `md:text-5xl`; dek switched from serif `text-xl` to plain sans `text-[15px]` `max-w-2xl`. (5) `IngestionBanner` collapsed to one quiet muted line (no icon, no all-caps label, no bg tint). (6) Ticker items: subtitle moved **below** the name (was opposite the severity pill, causing "PORTE-PAROLE / ÉNERGIE" overflow on the 380px column); narrative dek switched to sans (was serif). (7) Ticker count 6 → 5. Browser-verified visual cleanup; right-side dead space went from ~251px → ~29px (matches standard `px-6` gutter). (B) **Bilan Macron full re-verification + Macron-era reframing** (`src/data/bilan-macron.ts`): (1) Dispatched 7 parallel verification agents — each independently checked one section against Tier 1 official sources (INSEE, DREES, Eurostat, DGFiP, France Stratégie, Cour des comptes, HCC, Forbes/Challenges, Oxfam, EIU, RSF, CEVIPOF, Sénat McKinsey commission, HATVP, Tribunal admin Paris, IGPN/Dufresne, ANSES, Propluvia/BRGM, Santé publique France) plus Tier 1/2 press (AFP, Le Monde, France Info, Mediapart, Libération, Le Canard, Basta!). (2) ~30 fact corrections across all sections — examples: dette 117.4%→115.6% PIB (Q3 snapshot vs annual final), "Dividendes déclarés" relabeled "Dividendes versés CAC 40" (the figures were Vernimmen CAC 40, not DGFiP household), Div+rachats 45.8→50.9 Md (2017) and 108→107.5 Md (2025) with delta +135%→+111%, ISF cumul 25-30 Md→20 Md sur 7 ans, CTS 69→61 M collected and IPP estimate 3-6→1.15-3.9 Md, lits hôpital reframed (the original -30k claim was actually correct for 2017→2023 but had been mis-cited as 2013→2024), maternités 513/2013→435/2024, RSA conditioning 15-20h→15h, PISA -25→-21 pts, illectronisme "1/3"→15.4% INSEE 2021, Spinetta rapport (9k km menacés, 3.38k déjà fermés) replacing unsourced "6 615 km", Dufresne final tallies (24→30 éborgnés, 5→6 mains, 236→353 blessures tête), 2024 deaths 52 (Basta!) / 55 (Désarmons-les!), **typo fix "encerclement"→"désencerclement"**, dissolutions "33 (2017-2023), 40+ (2025)", milliardaires 32→39 (2017) and 52→53 (2026), Bernard Arnault baseline 41-85 Md→41.5 Md, "13 M/jour"→"+220 Md depuis 2017 (~67 M/jour)" (Oxfam Jan 2026), 51% pantouflage softened to "estimation presse", patrimoine ministres scoped to "gouvernement Bayrou", Cédric O "puis régulation IA"→"puis lobbying IA Act à Bruxelles" (defamation-sensitive), Leprettre's `from` field corrected (cabinet de Fesneau Relations Parlement, pas Agriculture) + Phyteis named explicitly, ENR objectif 32%→33%, dépression bracket 18-24→18-29 ans, suicide 3e cause→2e cause 15-24 ans (1re 25-34). (3) **Macron-era reframing** — user flagged that several baselines used 2013/2016 dates which is editorially wrong for a "Bilan Macron" dossier. Dispatched a 16-item agent to find verified 2017 baselines from Tier 1 sources, then rebased every metric: pauvreté 14.0%/2016→14.1%/2017, personnes 8.8M→8.9M (+1M→+900k), pauvreté infantile 19%/2016→20.1%/2017 (+3 pts→+1.8 pts), Gini 29.3/2016→28.8/2017, électricité 0.158/2016→0.1466/août 2017 (+60%→+72%), SMIC 1467/2016→1480/janv 2017 (+24%→+23%), dette 96%/2016→96.8%/2017 with +1350Md→+1282 Md, déficit 3.4%/2016→2.6%/2017 (+1.7→+2.5 pts), chômage BIT 10.2%/T1 2016→9.5%/T2 2017, jeunes 24%→22.3% (delta dropped from -2.5 to -0.8 pt — much less dramatic improvement under Macron), micro-entrepreneurs 1.07/fin 2016→1.18M/fin 2017, lits hôpital ~399k/fin 2017→369.4k/2023 with -30k delta, maternités 496/2017→435/2024 (-61), Democracy Index baseline 2016→2017, Restos du Cœur clarified as "campagne 2017-2018". Dropped the urgences entry entirely (DREES has no 2017 wave — only 2013 vs 2023). Dropped "doublé depuis 2012" tail on SDF entry. CONTRAST_ROWS synced to "30 000 lits depuis 2017". Only pre-2017 reference remaining: `mortsInterventions.hollande` 2012-2017 block (kept — comparative benchmark vs Macron's 288, not a tracked metric baseline). Browser-verified all 28 sentinel checks pass (old strings absent, new strings present). Build clean. Files touched: `src/app/page.tsx`, `src/app/layout.tsx` (Session 48 changes still uncommitted), `src/data/bilan-macron.ts`. Uncommitted. |
| **48** (Apr 21) | **Investigative front-page redesign** — Shifted the site's editorial register from "civic data browser" → "investigative publication" (ProPublica / Bellingcat / OCCRP / Le Monde Décodeurs). User shared a React mockup (`L'OEIL / Bureau des Données Publiques`) as the visual anchor; adapted to the real codebase preserving the "L'Observatoire" brand. (1) `src/app/page.tsx` full rewrite: `IngestionBanner` (latest HATVP IngestionLog row) → editorial 8/4 grid on `lg:`, left = `LeadStory` (Bilan Macron, serif 4xl-6xl headline, dek, rose "Enquête principale" label, `ProvenanceTag` credential strip) + 2 `SecondaryStoryCard` (Medias + Financement Politique, each with their own `ProvenanceTag`) + `InteractiveTeaser` (→ `/territoire`, teal-accented "Analyse interactive"), right = sticky `SignalTicker` (uses existing `getSignals()`, top 6 cards using `.severity-critique/.notable/.informatif` pills, signal `subtitle` in place of fake timestamps, footer shows total + critique count linking `/signaux`). (2) `src/app/layout.tsx`: new masthead `L'Observatoire // Bureau des données publiques` with inline eye-glyph SVG (replaces old pyramid triangle), 4-item nav `Dossiers | Signaux en Direct | Annuaire | Territoire` (dropped `/votes` and separate `Bilan Macron` item), new 3-column investigative footer (brand + tagline + version | Méthodologie | Accès) with honest `À venir` tooltips for unbuilt pages. (3) **LARP stripped from the mockup**: no `CELLULE D'INVESTIGATION` byline (no newsroom), no `DOSSIER : SIG-849-B` code (no case-ID system), no `CONF : ÉLEVÉE` (no methodology tiers yet). Only truthful editorial chrome kept. (4) No new components, no new routes, no schema changes, no new deps — all micro-components inline in the two files, all icons inline SVG (`lucide-react` NOT installed). All existing `globals.css` utilities reused (`.severity-*`, `.live-dot`). Color mapping: mockup `zinc-* → bureau-*`, `red-* → rose`, `font-serif → Instrument Serif`. (5) Verified: `pnpm build` clean, all 4 nav routes return 200, DOM contains all expected sections. Out of scope (deferred to follow-up PRs): `/signaux/[id]` case pages, provenance micro-component ripple site-wide, real `/methodologie/*` pages, network-graph viz, SecureDrop. Uncommitted. |
| **46** (Apr 21) | **HATVP Normalization + Re-ingestion** — User flagged that many Lecornu II ministers had finally filed HATVP declarations. Baseline check: 34/37 active ministers showed 0 declarations on their profiles; only 2 of 110 personnalites had any `InteretDeclare` rows. (1) **Refreshed HATVP merged XML**: downloaded current file from `hatvp.fr/livraison/merge/declarations.xml` (now 120MB, was 149MB — HATVP dropped retired officials from their merge). First download attempt via `ingest-declarations.ts` fetch() got cut off at 98MB; switched to `curl -C - --retry 30` which completed cleanly. Cache installed at `documentation/source-cache/declarations.xml`. Re-ran ingest: 10,226 declarations / 10,936 participations / 381,088 revenus, latest depot 2026-03-22. (2) **Root-caused the real bug**: profile pages showed empty not because declarations were missing but because name matching was broken. HATVP often drops accents (`NUNEZ` vs `Nuñez`, `Eleonore` vs `Éléonore`). Prisma `mode: "insensitive"` handles case but not diacritics. (3) **Full normalization**: added `nomNormalise` + `prenomNormalise` cols (compound btree-indexed) to 4 tables: `Depute`, `Senateur`, `DeclarationInteret`, `PersonnalitePublique`. Migration `20260421115129_add_normalized_name_columns`. Shared util `src/lib/normalize-name.ts` (NFD + strip U+0300-U+036F + lowercase + trim). Backfill script `scripts/backfill-normalized-names.ts` populated all 14,380 existing rows. Ingestion scripts (`ingest-deputes`, `ingest-senateurs`, `ingest-declarations`, `seed-gouvernement`) updated to populate on write. `ingest-hatvp.ts` patched to prefer local cached XML over URL (HATVP server is flaky). (4) **Re-ran `ingest-hatvp.ts`**: **InteretDeclare jumped from 184 rows / 2 personnalites to 1,988 rows / 67 personnalites**. **29 of 37 active Lecornu II ministers** now have parsed declarations (Nuñez 10 interests, Geffray 70, Caroit 8, Darmanin 55, Vautrin 66). The 8 still at 0 are genuinely unfiled (Macron + 7 secretaries). (5) **App queries updated**: swapped all `{ equals, mode: "insensitive" }` name matches for `nomNormalise`/`prenomNormalise` equality in 5 files (`profils/deputes/[id]`, `profils/senateurs/[id]`, `president`, `profils/[slug]`, `profils/comparer`). Also removed a dead `declarationCount` query in comparer that was matching empty strings. Build clean. **Not yet committed** — the 109 `data/research-output/*.json` files from Session 44 are still untracked alongside these Session 46 changes. |
| **45** (Mar 27) | **Bilan Macron Investigation Dossier** — Deep research (4 parallel agents, 150+ web searches across INSEE, Eurostat, DREES, France Stratégie, Oxfam, CEVIPOF, Amnesty International, RSF, EIU, Fondation Abbé Pierre) into the socioeconomic impact of Macron's two presidential terms (2017-2026). New route `/dossiers/bilan-macron` with 7 section components: (1) `BilanHeroSection` — 4 headline stat cards (+1M poverty, +1,350 Md debt, billionaires x2, -30K hospital beds) + Bocquet "évaporation" quote. (2) `BilanEconomieSection` — before/after tables for poverty (14.0%→15.4%, +1M people), purchasing power (+20% inflation, electricity +60%), debt (+63%, deficit 5.1%), fiscal gifts (ISF ~35 Md EUR lost, CICE 900K EUR/job, "zero effect" per France Stratégie), employment (headline down, youth down from ~24% to 21.5% but recent spike, micro-entrepreneurs doubled). (3) `BilanSanteSection` — 30K hospital beds closed (trend since early 2000s), 87% medical deserts, 5 assurance chômage reforms (1M lost benefits), APL cuts, RSA conditioned (Défenseur des droits: "violates rights"), 10K+ teaching positions cut, PISA historic drop. (4) `BilanDroitsSection` — police violence (2,500 GJ injured, 24 eyes lost, 288 deaths 2018-2024 vs 92 under Hollande), ~32x 49.3 (Borne alone: 23, record since 2008 reform), democracy downgraded to "flawed", 33 association dissolutions, ordonnances travail, environment (France condemned twice, only EU country to miss 2020 renewables target). (5) `BilanElitesSection` — 32→52 billionaires, top 500 fortunes x2.15 (571→1,228 Md EUR), McKinsey 2.4 Md EUR (zero tax), 51% ministers to private sector. (6) `BilanContrasteSection` — side-by-side "Les deux Frances" grid (8 rows, 3 columns). (7) Sources footer (16 official sources). Static data file `src/data/bilan-macron.ts` (~740 lines, typed constants with source citations). Added to `dossier-config.ts` + `DossierNav`. Added "Bilan Macron" as 5th main nav item in `layout.tsx`. French accents fixed throughout (data file + all 7 components + page footer). **Fact-check verification** (3 parallel agents, 30+ web searches): corrected billionaire multiplier x2.5→x2 (1,228/571=2.15, Challenges confirmed), 49.3 Borne 19→23 (France Info confirmed), 49.3 total ~31→~32 (Bayrou used 4 not 3), youth unemployment added 2016 baseline ~24% for context, dividends source DGFiP→Vernimmen/CAC 40, hospital beds "since 2013"→"since early 2000s" (DREES/IRDES: trend since ~2003), deficit kept at 5.1% (INSEE published 5.1% on Mar 27 2026, not 5.4% as initially suggested). Build clean. All corrections browser-verified. |
| **44** (Mar 27) | **Deep Government Investigation — Lecornu II** — Full transparency investigation of all 37 ministers via 12 parallel web-research agents (~200+ web searches). (1) **Phase 1 — Judicial deep dive**: New EvenementJudiciaire: Darmanin CJR complaint for Sarkozy prison visit (classement sans suite, Dec 2025). Tabarot PNF enquete confirmed (detournement de fonds publics, ongoing since Sept 2021). 8 new conflict flags across 8 ministers (Darmanin structural conflict, Farandou/Papin official recusal decrees, Lescure tax haven investments, Rist 309 pharma links, Bregeon EDF rapporteur, Pegard Epstein photo + Cour des comptes criticism, Agresti-Roubache HATVP gap, Genevard FNSEA proximity, Barbut WWF revolving door). (2) **Phase 2 — Revolving door documentation**: 6 PORTE_TOURNANTE flags with AGORA lobby data: Bregeon/EDF (130 lobby actions), Farandou/SNCF (75), Lescure/Natixis+Groupama (33), Baptiste/IBM+Total (11), Papin/Systeme U, Pegard/Le Point. (3) **Phase 3 — Vote contradiction analysis**: `analyze-vote-contradictions.ts` queries VoteRecord for 17 deputy-ministers. Top findings: Lefevre 81.5% contre on ecology, Galliard-Minier 68.4% on health, Amiel 40.4% on budget. 10 VOTE_CONTRADICTION flags added to JSONs. (4) **Phase 4 — Lobby exposure**: `analyze-lobby-exposure.ts` maps 131,842 ActionLobby by ministereCode. Career-lobby overlaps: EDF→130 actions on Bregeon's ministry, SNCF→75 on Farandou's, Natixis+Groupama→33 on Lescure's. (5) **Phase 5 — Ingestion**: All findings ingested, career timelines regenerated, search index refreshed. (6) **Phase 6 — UI conflict indicators**: CareerSection gains revolving door amber "Porte tournante" badge via `matchRevolvingDoor()`. ParliamentarySection gains "Votes en tension avec le portefeuille actuel" alert with count badge + example votes. LobbySection gains "Ancien employeur" highlight when top lobbying org matches career org. `PORTFOLIO_KEYWORDS` expanded with Lecornu II-specific companies (EDF, SNCF, Natixis, Systeme U, IBM, Total, Le Point, Versailles). `MINISTERECODE_TO_TAGS` mapping added to ParliamentarySection. Browser-tested: Bregeon Porte tournante badge, Rist vote contradiction alert (67/395), Darmanin 4 judicial events, /signaux 77 signals (21 portes tournantes). Build clean. |
| **43** (Mar 26) | **UX Restructure Phases 4-6 + Scrutin Refresh** — Extracted `src/lib/vote-tags.ts` (shared TAG_LABELS/ORDER/COLORS/VALID); fixed all legacy route links in 22 files; moved `/economie` → `/territoire/economie` and `/comparer/territoires` → `/territoire/comparer`; created `/votes/scrutins/[id]` as canonical scrutin detail; activated all redirects in `next.config.ts` (14 `/representants/*`, 2 `/gouvernement/*`, 4 scrutin, 1 `/comparer/deputes`); deleted 29 legacy pages (-6,203 lines net). Ingested 1,140 new scrutin JSONs (5,831 scrutins + ~973K votes total, Oct 2024 – Mar 2026). |
| **42** (Mar 26) | **HATVP Financial Declaration Audit** — `scripts/lib/hatvp-parser.ts` extracted (10 functions); `scripts/audit-declarations.ts` 3-phase audit (XML vs DB 1,973/1,973 perfect, internal consistency 0 divergences). Fresh XML re-ingest (~500K rows). Senator case-sensitivity fix unlocked 471 unreachable declarations. |
| **41** (Mar 26) | **Voting Overhaul + Audit + Data Accuracy** — `ScrutinAccordion` redesigned to role-grouped collapsible sections; scrutin detail gained parent-law breadcrumb; `/votes/par-sujet/[tag]` defaults to `?vue=final` excluding amendments. Signal severity recalibrated (lobbySeverity 10K→5K). 20/21 minister `formation` fields corrected against verified sources. AGORA ingestion wrapped in `prisma.$transaction()`. |
| **40** (Mar 26) | **UX Restructure Phases 1-3** — Nav 9→4; homepage rewrite (search-first hero, inline signals, recent votes, territory lookup; killed HeroSlider/dossier grid/KPI counters); new `/profils/*` hub for all people (deputes/senateurs/ministres/lobbyistes/partis/elus/comparer); killed 8 dossier pages; `/signaux` URL filters; `src/lib/slug.ts` utility. |
| **39** (Mar 25) | **UX/UI Audit + 7 Fixes** — Accent fixes on `/dossiers/medias`; PIB display bug (3 Md → 2,639 Md); SMIC unit `eur`→`indice`; dette publique unit corrected; homepage stat cards clickable; `DossierNav` overflow fade; museum capitalization helper; AN photo URLs confirmed 404 (Avatar initials fallback correct). |
| **38** (Mar 25) | **Browser QA + Fixes** — `mode: "insensitive"` on declaration matching (precursor to Session 46 normalization); SMIC BDM series labelled as index; `Gouvernement` added to main nav (8 items at the time); `Représentants` breadcrumb fix. Plus `/signaux` cross-dataset intelligence (6 signal types, query-time logic, no new models). |
| **37** (Mar 25) | **Transparency Cross-References** — `ConflictDrilldown` client component (deputy Transparence tab); senator Transparence tab (4th, commission-lobbying overlap via `COMMISSION_DOMAINS`); historical govt data files (Borne 36 / Attal 35 / Barnier 41 — seed not yet run); `/dossiers/medias` "Connexions politiques" + AGORA Culture overlap; `nuance-party-map.ts` + `/dossiers/financement-politique` dossier. |
| **36** (Mar 8) | Judicial deep dive on Lecornu II — Bergé upgraded to CJR; Jeanbrun gained 2nd entry (PNF CITALLIOS). `/gouvernement` SIGINT redesign (now superseded by `/profils` consolidation Session 43). |
| **35** (Mar 6) | Media Ownership dossier — 4 new Prisma models (`GroupeMedia`, `MediaProprietaire`, `ParticipationMedia`, `Filiale`); `/dossiers/medias` route; `MobileNav` hamburger added. |
| **34** (Mar 5) | Lecornu II rework — Bayrou mandates closed; 37 members seeded; AGORA scaled to 20 ministry codes (131,842 ActionLobby); 38 research-output JSONs → 474 career + 13 judicial events. |
| **33** (Mar 4) | **Parliamentary Laws Hub** — `LoiParlementaire` + `ScrutinLoi` models, 19 lois seeded, 2,589 links. `/votes/lois` + `/votes/lois/[slug]` routes; `LoiCard`, `GroupExpander`, `ScrutinAccordion` components. |
| **21–32** (Mar 3-4) | Phase 8 (FranceMap component + 7-page integration) and Phase 9A-9F (Government Profiles schema + UI + AGORA + career timeline + research-output workflow + judicial section). See archive for detail. |
| **18–20** (Mar 3) | Phase 7E/7F QA — housing data, OG images, party page crash fix. |

Full session history (Sessions 1–20): [`documentation/implemented-plans/session-history-phases-1-7.md`](implemented-plans/session-history-phases-1-7.md)
